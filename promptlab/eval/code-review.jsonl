{"role": "code-review", "scenario": "invoicely-saas", "system": "You are the code-review agent for Archon, operating on the invoicely-saas repository (TypeScript · React 18 + Vite · Node/Express · PostgreSQL + Prisma · Stripe · pdf-lib). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. invoicely-saas CLAUDE.md rules: Prisma migrations in db/migrations/; controllers in src/api/controllers/; zod schemas for all request bodies; monetary values as integer cents; tenantId injected by auth middleware into req.tenantId; no raw SQL — Prisma only; Jest + Supertest for integration tests.", "user": "Review PR #482 against invoicely-saas CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nResolved node inputs:\n- $ARTIFACTS_DIR/.pr-number => 482\n- scope.md 'NOT Building (Scope Limits)': distributed/Redis-backed rate limiting (in-memory placeholder is acceptable for this PR); pagination of search results.\n- PR diff (src/api/controllers/clientController.ts):\n```diff\n+const loginAttempts: Record<string, number> = {};\n+export async function searchClients(req, res) {\n+  const ip = req.ip;\n+  loginAttempts[ip] = (loginAttempts[ip] || 0) + 1;\n+  if (loginAttempts[ip] > 100) return res.status(429).json({ error: 'Too many requests' });\n+  const { q } = req.query;\n+  const clients = await prisma.$queryRawUnsafe(`SELECT * FROM \"Client\" WHERE name ILIKE '%${q}%' AND tenant_id = '${req.tenantId}'`);\n+  res.json(clients);\n+}\n+export async function deleteClient(req, res) {\n+  const { id } = req.params;\n+  prisma.client.delete({ where: { id, tenantId: req.tenantId } });\n+  res.status(204).send();\n+}\n```", "gold": "# Code Review Findings: PR #482\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T14:22:10Z\n**Files Reviewed**: 1\n\n## Summary\nThis PR adds `searchClients` and `deleteClient` to the client controller but ships a SQL-injection vector via raw string interpolation and a missing `await` that silently drops delete failures. Both directly violate invoicely-saas CLAUDE.md (\"no raw SQL — Prisma only\") and core async correctness. The in-memory rate-limit `ip` quirk is real but lives partly under a scope exclusion, so only its unbounded-growth aspect is flagged.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: SQL injection via $queryRawUnsafe string interpolation\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `src/api/controllers/clientController.ts:14`\n**Issue**: The search query interpolates user-controlled `q` (and `tenantId`) directly into a raw SQL string, allowing injection and cross-tenant data exfiltration. It also violates the \"Prisma only, no raw SQL\" rule.\n**Evidence**:\n```ts\nconst clients = await prisma.$queryRawUnsafe(\n  `SELECT * FROM \"Client\" WHERE name ILIKE '%${q}%' AND tenant_id = '${req.tenantId}'`\n);\n```\n**Why This Matters**: A `q` value like `' OR '1'='1` returns every tenant's clients. In a multi-tenant billing product this is a direct PII/data-integrity breach.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Use Prisma `findMany` with `contains` filter | No raw SQL, matches CLAUDE.md, tenant-scoped via where | Slightly different ILIKE semantics (add `mode: 'insensitive'`) |\n| B | Keep raw SQL but use `Prisma.sql` parameterized `$queryRaw` tagged template | Parameterized, injection-safe | Still raw SQL — violates \"Prisma only\" rule |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md mandates Prisma-only; `findMany` with `mode: 'insensitive'` is injection-proof by construction and keeps tenant scoping explicit, matching the existing controller pattern.\n**Recommended Fix**:\n```ts\nconst clients = await prisma.client.findMany({\n  where: { tenantId: req.tenantId, name: { contains: String(q ?? ''), mode: 'insensitive' } },\n});\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/api/controllers/invoiceController.ts:38-44\nconst invoices = await prisma.invoice.findMany({\n  where: { tenantId: req.tenantId, status: { in: ['DRAFT', 'SENT'] } },\n});\n```\n\n### Finding 2: Missing await on prisma.client.delete swallows errors\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/api/controllers/clientController.ts:21`\n**Issue**: `prisma.client.delete(...)` is not awaited, so the 204 is sent before the delete resolves; a rejected promise (e.g., record not found, FK constraint) becomes an unhandled rejection and the client believes the delete succeeded.\n**Evidence**:\n```ts\nprisma.client.delete({ where: { id, tenantId: req.tenantId } });\nres.status(204).send();\n```\n**Why This Matters**: Users see success while the row may still exist, and failures are invisible — exactly the silent-data-integrity class flagged in the open priority-high issue.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `await` the delete inside try/catch, map P2025 to 404 | Correct status, no swallowed errors | A few more lines |\n| B | `await` only, let a central error middleware handle rejection | Minimal diff | Relies on middleware existing/正确 mapping |\n**Recommended**: Option A\n**Reasoning**: Explicit await + P2025→404 matches how the codebase reports not-found and prevents the false-204; central middleware mapping is not guaranteed for Prisma error codes.\n**Recommended Fix**:\n```ts\ntry {\n  await prisma.client.delete({ where: { id, tenantId: req.tenantId } });\n  res.status(204).send();\n} catch (err) {\n  if ((err as { code?: string }).code === 'P2025') return res.status(404).json({ error: 'Client not found' });\n  throw err;\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/api/controllers/invoiceController.ts:71-76\nawait prisma.invoice.delete({ where: { id, tenantId: req.tenantId } });\nres.status(204).send();\n```\n\n### Finding 3: In-memory loginAttempts map grows unbounded\n**Severity**: MEDIUM\n**Category**: performance\n**Location**: `src/api/controllers/clientController.ts:4`\n**Issue**: `loginAttempts` is keyed by IP and never pruned or reset, so it grows for the process lifetime — a slow memory leak. (The single-process / non-distributed nature of this store is explicitly out of scope, so only unbounded growth is flagged.)\n**Evidence**:\n```ts\nconst loginAttempts: Record<string, number> = {};\nloginAttempts[ip] = (loginAttempts[ip] || 0) + 1;\n```\n**Why This Matters**: Long-running Node processes accumulate one entry per unique IP forever, degrading memory over weeks.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Use a windowed counter with periodic reset / TTL Map | Bounds memory, keeps placeholder simple | Minor timer/cleanup logic |\n| B | Swap to express-rate-limit middleware | Battle-tested, auto-expiry | Larger change; distributed store still out of scope |\n**Recommended**: Option A\n**Reasoning**: Stays within the \"in-memory placeholder acceptable\" scope while fixing the only real defect (no expiry); a TTL reset matches the lightweight intent without pulling in new deps.\n**Recommended Fix**:\n```ts\nlet windowStart = Date.now();\nfunction hit(ip: string) {\n  if (Date.now() - windowStart > 60_000) { for (const k in loginAttempts) delete loginAttempts[k]; windowStart = Date.now(); }\n  return (loginAttempts[ip] = (loginAttempts[ip] || 0) + 1);\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/cache.ts:12-19 (existing windowed-reset pattern)\nif (Date.now() - this.start > this.ttlMs) { this.store.clear(); this.start = Date.now(); }\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | No |\n| HIGH | 1 | Yes |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | — |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n| No raw SQL — Prisma only | FAIL | $queryRawUnsafe used in searchClients (Finding 1) |\n| Controllers in src/api/controllers/ | PASS | File is correctly located |\n| zod schemas for request bodies | FAIL | No zod validation for `q` query param |\n| tenantId from req.tenantId | PASS | Both handlers scope by req.tenantId |\n| Monetary values as integer cents | N/A | No monetary fields touched |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/api/controllers/invoiceController.ts | 38-44 | Tenant-scoped Prisma findMany |\n| src/api/controllers/invoiceController.ts | 71-76 | Awaited delete + 204 |\n| src/lib/cache.ts | 12-19 | Windowed in-memory reset |\n\n## Positive Observations\n- Both handlers correctly scope queries by `req.tenantId`, honoring multi-tenant isolation.\n- The controller is placed in the canonical `src/api/controllers/` directory.\n- A 429 path for rate limiting is at least present, giving a clean hook to harden later.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T14:22:10Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "wayfarer-collab-api", "system": "You are the code-review agent for Archon, reviewing pull requests for the \"wayfarer-collab-api\" project (a real-time collaborative travel-itinerary backend on TypeScript / Express 5 / Drizzle / PostgreSQL / Redis / Socket.IO / Zod). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, plus scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's \"NOT Building\" exclusion list. Verdict must be exactly one of APPROVE, REQUEST_CHANGES, or NEEDS_DISCUSSION.", "user": "MISSION: Review the PR diff against wayfarer-collab-api's CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding give severity, location, evidence, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nResolved node inputs:\n- PR number ($ARTIFACTS_DIR/.pr-number): 142\n- scope.md \"NOT Building (Scope Limits)\": presence TTL tuning; rate-limit config changes; migrating presence off Redis sets.\n- CLAUDE.md conventions: Express handlers async with central error-handling middleware (no try/catch in routes); all Socket.IO handlers Zod-validated; Redis keys namespaced (presence:, lock:, pubsub:); NEVER store user PII in Redis (only user IDs); rate limiting on all REST endpoints; integration tests in tests/integration/ with real test DB.\n\nPR diff (src/routes/presence.ts):\n```diff\n const router = Router()\n+const heartbeatSchema = z.object({ userId: z.string(), color: z.string() })\n+const presenceStore: Record<string, { userId: string; lastSeen: number }[]> = {}\n router.get('/:tripId/presence', async (req, res) => {\n   const { tripId } = req.params\n+  if (presenceStore[tripId]) { return res.json({ members: presenceStore[tripId] }) }\n+  const raw = await redis.smembers(`presence:${tripId}`)\n+  presenceStore[tripId] = raw.map(r => JSON.parse(r))\n+  res.json({ members: presenceStore[tripId] })\n })\n router.post('/:tripId/heartbeat', async (req, res) => {\n+  const parsed = heartbeatSchema.safeParse(req.body)\n+  if (!parsed.success) return res.status(400).json({ error: 'Invalid body' })\n+  const { userId } = parsed.data\n+  const ip = req.ip\n   await redis.sadd(`presence:${tripId}`, JSON.stringify({ userId, ip, lastSeen: Date.now() }))\n   await redis.expire(`presence:${tripId}`, 30)\n })\n```", "gold": "# Code Review Findings: PR #142\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:30:00Z\n**Files Reviewed**: 1\n\n## Summary\nThis PR adds a Zod-validated heartbeat and an in-memory presence cache. It introduces a CLAUDE.md PII violation (storing `req.ip` in Redis), an unbounded module-level cache that breaks horizontal scaling and never invalidates, and an unhandled `JSON.parse`. The intended GET-from-Redis reliability fix is not actually addressed.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: User IP stored in Redis (PII violation)\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `src/routes/presence.ts:heartbeat`\n**Issue**: `req.ip` is persisted into a Redis presence value. CLAUDE.md forbids any user PII in Redis; only user IDs are permitted. IP is PII.\n**Evidence**:\n```ts\nconst ip = req.ip\nawait redis.sadd(`presence:${tripId}`, JSON.stringify({ userId, ip, lastSeen: Date.now() }))\n```\n**Why This Matters**: Leaks PII into a shared cache layer not covered by data-retention controls; compliance and security exposure.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Drop `ip` entirely | Fully compliant, minimal | Loses any debug signal |\n| B | Store only `userId` + `color` from schema | Compliant, keeps UX color | Slightly more shape work |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md is absolute on PII in Redis; `ip` serves no acceptance-criteria purpose. Option B is acceptable but `color` wasn't previously persisted.\n**Recommended Fix**:\n```ts\nawait redis.sadd(`presence:${tripId}`, JSON.stringify({ userId, lastSeen: Date.now() }))\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/redis.ts:40 — only userId stored\nawait redis.sadd(`presence:${tripId}`, JSON.stringify({ userId }))\n```\n\n### Finding 2: Module-level presenceStore breaks multi-instance correctness\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/routes/presence.ts:presenceStore`\n**Issue**: `presenceStore` is process-local and never invalidated, so it serves stale data across instances and ignores Redis TTL expiry. Labeled NEW abstraction (grep found no existing in-memory cache).\n**Evidence**:\n```ts\nif (presenceStore[tripId]) { return res.json({ members: presenceStore[tripId] }) }\n```\n**Why This Matters**: With multiple Node instances behind a load balancer, presence is wrong and members never disappear after heartbeat expiry.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Remove cache, read Redis each request | Correct, simple | Slightly more Redis calls |\n| B | Short-TTL cache (1-2s) | Some speed | Still cross-instance stale within window |\n**Recommended**: Option A\n**Reasoning**: Redis sets are already fast; correctness in a collaborative system outranks micro-latency. The cache also masks the reliability bug the issue actually requests.\n**Recommended Fix**:\n```ts\nconst raw = await redis.smembers(`presence:${tripId}`)\nres.json({ members: raw.map(r => JSON.parse(r)) })\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/trips.ts:88 — stateless read straight from store\nconst rows = await db.select().from(trips)\n```\n\n### Finding 3: Unguarded JSON.parse on Redis values\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `src/routes/presence.ts:GET`\n**Issue**: `JSON.parse(r)` will throw on any malformed member, bubbling an unhandled error.\n**Evidence**:\n```ts\npresenceStore[tripId] = raw.map(r => JSON.parse(r))\n```\n**Why This Matters**: One corrupt entry fails the whole request.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | safeParse helper, skip bad rows | Resilient | Silent drop |\n| B | Validate with Zod member schema | Strong typing | More code |\n**Recommended**: Option B\n**Reasoning**: Project already standardizes on Zod for boundary parsing; reuse keeps it consistent (EXTENDS existing Zod usage).\n**Recommended Fix**:\n```ts\nconst members = raw.flatMap(r => { const p = memberSchema.safeParse(JSON.parse(r)); return p.success ? [p.data] : [] })\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/presence.ts:heartbeat — boundary Zod parse\nconst parsed = heartbeatSchema.safeParse(req.body)\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | Yes |\n| HIGH | 1 | Yes |\n| MEDIUM | 1 | Partial |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| No PII in Redis | FAIL | `req.ip` stored (Finding 1) |\n| Zod-validate inputs | PASS | heartbeat now validated |\n| No try/catch in routes | PASS | none added |\n| Redis key namespacing | PASS | `presence:` prefix kept |\n| Rate limiting on endpoints | N/A | not changed by diff |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/lib/redis.ts | 40 | userId-only presence value |\n| src/routes/trips.ts | 88 | stateless store read |\n| src/routes/presence.ts | heartbeat | boundary Zod parse |\n\n## Positive Observations\n- Heartbeat body is now Zod-validated, closing an unvalidated-input gap.\n- Redis key namespacing (`presence:`) preserved correctly.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T18:30:00Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "tapecast-feed", "system": "You are the code-review agent for Archon, reviewing PRs in the tapecast-feed repo (TypeScript · Fastify · Drizzle ORM · PostgreSQL · Cloudflare Workers · Vitest · Zod). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. tapecast-feed conventions: no ORM queries outside repository classes in src/repositories/; route handlers typed with Zod; subscriber tracking is privacy-first (IP never stored; SHA-256 hash of ip+salt+date stored as subscriber_hash); CI requires 100% of changed files to have tests.", "user": "Mission: Review PR #412 against tapecast-feed's CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding, provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix with reasoning, and a codebase pattern reference. Conclude with verdict, statistics table, CLAUDE.md compliance table, patterns referenced, and positive observations.\n\n$ARTIFACTS_DIR/.pr-number => 412\nPR #412 title: Fix feed download counter double-counting Range requests (closes #389)\n\nscope.md (excerpt):\n  In scope: dedupe download counting by subscriber_hash per episode per calendar day; exclude partial-content Range requests except initial full-file requests.\n  NOT Building (Scope Limits): webhook delivery (separate PR #420); Cloudflare edge-cache invalidation; historical backfill of inflated counts.\n\nPR diff:\n--- a/src/routes/feed.ts\n+++ b/src/routes/feed.ts\n@@\n+import crypto from 'node:crypto';\n+const downloadCache: Record<string, number> = {};\n   fastify.get('/podcasts/:podcastSlug/episodes/:episodeId/audio', async (request, reply) => {\n     const { podcastSlug, episodeId } = request.params as { podcastSlug: string; episodeId: string };\n+    const ip = request.headers['x-forwarded-for'] as string;\n     const rangeHeader = request.headers['range'];\n     const episode = await new FeedRepository().findEpisode(podcastSlug, episodeId);\n     if (!episode) return reply.code(404).send();\n-    const isCountable = !rangeHeader || rangeHeader.startsWith('bytes=0-');\n+    const isCountable = !rangeHeader || rangeHeader.startsWith('bytes=0');\n     if (isCountable) {\n-      const salt = process.env.SUBSCRIBER_SALT!;\n+      const salt = process.env.SUBSCRIBER_SALT;\n       const today = new Date().toISOString().slice(0, 10);\n       const hash = crypto.createHash('sha256').update(ip + salt + today).digest('hex');\n-      await new DownloadRepository().recordDownload(episodeId, hash);\n+      const cacheKey = `${episodeId}:${hash}`;\n+      if (!downloadCache[cacheKey]) {\n+        downloadCache[cacheKey] = Date.now();\n+        new DownloadRepository().recordDownload(episodeId, hash);\n+      }\n     }\n     const stream = await new FeedRepository().getAudioStream(episode.s3Key);\n     return reply.send(stream);\n   });", "gold": "# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T00:00:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe PR addresses #389 (Range double-counting) but introduces a process-lifetime memory leak via an unbounded module-level cache, breaks the salt non-null guarantee causing hash collisions, floats an un-awaited DB write, and weakens both the Range filter and IP source. The dedupe intent is correct but the implementation regresses correctness and reliability.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Unbounded module-level downloadCache leaks memory\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/routes/feed.ts:2`\n**Issue**: `downloadCache` grows forever; every calendar day mints new hashes for every (episode, listener), and nothing evicts. A long-running Fastify process trends to OOM.\n**Evidence**:\n```ts\nconst downloadCache: Record<string, number> = {};\n```\n**Why This Matters**: Silent OOM in production; dedupe is also not durable across instances, so it doesn't actually replace DB-level dedupe.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Drop the in-memory cache; enforce dedupe with a unique index `(episode_id, subscriber_hash, day)` and `onConflictDoNothing` | Durable, multi-instance-correct, no leak | Requires migration |\n| B | Keep cache but bound it with a TTL/LRU | Smaller change | Still per-instance, not authoritative; extra dep |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md mandates dedupe by subscriber_hash per episode per day — that is a data invariant and belongs in Postgres, not process memory. Matches existing repository-owned constraint patterns.\n**Recommended Fix**:\n```ts\nawait new DownloadRepository().recordDownload(episodeId, hash); // dedupe via unique index in repo\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/db/schema.ts:88-94 — unique() composite index used for idempotent writes\nexport const subscriberDays = pgTable('subscriber_days', { /* ... */ }, (t) => ({ uniq: unique().on(t.episodeId, t.subscriberHash, t.day) }));\n```\n\n### Finding 2: SUBSCRIBER_SALT non-null assertion removed → hash collision\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `src/routes/feed.ts` (salt assignment)\n**Issue**: If `SUBSCRIBER_SALT` is unset, `salt` is `undefined`; `update(ip + undefined + today)` coerces to the literal `\"undefined\"`, so all listeners that day collapse to identical hashes and dedupe counts at most one download per episode.\n**Evidence**:\n```ts\nconst salt = process.env.SUBSCRIBER_SALT;\n```\n**Why This Matters**: Silently corrupts analytics in the opposite direction (under-counting) and defeats the privacy hash's purpose.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Fail fast at boot: validate env with Zod and throw if missing | Centralized, matches Zod convention | Touches startup config |\n| B | Restore `!` non-null assertion inline | Minimal diff | No real guarantee; silently re-breaks if env drifts |\n**Recommended**: Option A\n**Reasoning**: tapecast-feed already validates inputs with Zod; a required secret should be validated at startup so the failure is loud, not a silent analytics bug.\n**Recommended Fix**:\n```ts\n// config.ts\nexport const env = z.object({ SUBSCRIBER_SALT: z.string().min(16) }).parse(process.env);\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/config.ts:5-12 — env parsed/validated once via Zod at boot\nexport const env = envSchema.parse(process.env);\n```\n\n### Finding 3: Floating, un-awaited recordDownload\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/routes/feed.ts` (recordDownload call)\n**Issue**: `new DownloadRepository().recordDownload(...)` is no longer awaited; rejections (unique violation, connection drop) are unhandled and lost.\n**Evidence**:\n```ts\nnew DownloadRepository().recordDownload(episodeId, hash);\n```\n**Why This Matters**: Silent data loss and unhandled-rejection noise; in newer Node an unhandled rejection can crash the process.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await` the call inside try/catch with structured log | Errors observable, write guaranteed before response | Adds slight latency to response |\n| B | Fire-and-forget but attach `.catch(log)` | Non-blocking | Write not guaranteed before reply; weaker ordering |\n**Recommended**: Option A\n**Reasoning**: A counted download must be persisted reliably; the audio stream send is fast, so awaiting the write is acceptable and matches the awaited `findEpisode` call already in the handler.\n**Recommended Fix**:\n```ts\ntry { await new DownloadRepository().recordDownload(episodeId, hash); }\ncatch (err) { request.log.error({ err, episodeId }, 'recordDownload failed'); }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/feed.ts:13 — sibling repo call is awaited\nconst episode = await new FeedRepository().findEpisode(podcastSlug, episodeId);\n```\n\n### Finding 4: `startsWith('bytes=0')` is too broad\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `src/routes/feed.ts` (isCountable)\n**Issue**: `'bytes=0'` matches `bytes=0-500` (a 500-byte duration probe) and `bytes=00-`, counting partial probes as full downloads — partially re-opening #389.\n**Evidence**:\n```ts\nconst isCountable = !rangeHeader || rangeHeader.startsWith('bytes=0');\n```\n**Why This Matters**: RFC 3005 / issue #389 require counting only full downloads or initial-range requests starting at byte 0 for the whole file.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Restore `startsWith('bytes=0-')` | One-char fix, correct for full-range resume | Still counts `bytes=0-500` probes as countable |\n| B | Parse the range; count only open-ended `bytes=0-` (no end) | Precisely excludes byte probes | Slightly more code |\n**Recommended**: Option B\n**Reasoning**: The issue explicitly calls out duration-probe Range requests; matching only the open-ended `bytes=0-` form excludes probes while honoring resumable full downloads.\n**Recommended Fix**:\n```ts\nconst isCountable = !rangeHeader || /^bytes=0-$/.test(rangeHeader.trim());\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/http.ts:22-27 — header parsing uses anchored regex, not startsWith\nexport const isFullRange = (h?: string) => !h || /^bytes=0-$/.test(h);\n```\n\n### Finding 5: Raw x-forwarded-for trusted for client IP\n**Severity**: MEDIUM\n**Category**: security\n**Location**: `src/routes/feed.ts` (ip assignment)\n**Issue**: Behind Cloudflare the trustworthy client IP is `cf-connecting-ip`; raw `x-forwarded-for` is client-controllable, so a caller can rotate the header and inflate counts past dedupe.\n**Evidence**:\n```ts\nconst ip = request.headers['x-forwarded-for'] as string;\n```\n**Why This Matters**: Defeats per-listener dedupe and lets analytics be gamed; also risks hashing a comma-joined XFF list.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Use `cf-connecting-ip`, fall back to leftmost trusted XFF | Correct for CF deployment | CF-specific |\n| B | Enable Fastify `trustProxy` and use `request.ip` | Framework-native, generic | Needs proxy config to be accurate |\n**Recommended**: Option A\n**Reasoning**: The stack runs behind Cloudflare; `cf-connecting-ip` is the documented authoritative client IP and cannot be spoofed past CF.\n**Recommended Fix**:\n```ts\nconst ip = (request.headers['cf-connecting-ip'] as string) ?? request.ip;\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/plugins/clientIp.ts:8 — existing helper already prefers cf-connecting-ip\nexport const clientIp = (req: FastifyRequest) => req.headers['cf-connecting-ip'] as string ?? req.ip;\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | No |\n| HIGH | 2 | Partial |\n| MEDIUM | 2 | Yes |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| No ORM queries outside src/repositories/ | PASS | Uses DownloadRepository/FeedRepository |\n| Dedupe by subscriber_hash per episode per day | FAIL | Done in volatile process memory, not durably |\n| IP never stored; SHA-256(ip+salt+date) | PARTIAL | Hash used, but undefined salt collapses hashes |\n| 100% changed files have tests | FAIL | No test added for feed.ts in this PR |\n| Zod-typed inputs / validated config | FAIL | Secret read unvalidated from process.env |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/db/schema.ts | 88-94 | Composite unique index for idempotent writes |\n| src/config.ts | 5-12 | Zod env validation at boot |\n| src/routes/feed.ts | 13 | Awaited repository call |\n| src/lib/http.ts | 22-27 | Anchored regex range parsing |\n| src/plugins/clientIp.ts | 8 | cf-connecting-ip helper |\n\n## Positive Observations\n- Correctly scopes counting logic to a single guarded branch and keeps DB access inside repository classes.\n- Reuses the existing SHA-256(ip+salt+date) privacy scheme rather than storing raw IPs.\n- Targets the real root cause from #389 (Range requests) rather than masking the symptom downstream.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T00:00:00Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "academytrack-lms-mobile", "system": "You are the code-review agent for Archon, reviewing a pull request in the academytrack-lms-mobile repo (TypeScript / NestJS / Vue 3 / TypeORM-MySQL / Capacitor). Your job is to review the PR for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff via `gh pr diff`, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns via grep, check for primitive duplication labeled EXTENDS/DUPLICATE/NEW), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide at least two fix options with pros/cons plus an explicit recommended option. You never flag items listed in the scope's 'NOT Building (Scope Limits)' exclusion list. Verdict must be exactly one of APPROVE, REQUEST_CHANGES, or NEEDS_DISCUSSION.", "user": "Mission: Review the PR diff for the per-department compliance CSV export feature against academytrack-lms-mobile's CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nResolved node inputs:\n- PR number (from $ARTIFACTS_DIR/.pr-number): 412\n- Feature: compliance officer exports per-department completion report CSV (employee_id, employee_name, department, course_id, course_name, status, completion_date, days_overdue). Async via BullMQ, presigned S3 URL, must respect row-level-security subtree scope, max 50k rows, results expire 24h.\n- scope.md 'NOT Building (Scope Limits)': PDF export format; pagination of the status endpoint; configurable column selection; localization of CSV headers.\n- PR diff under review:\n```diff\n--- a/src/reports/reports.service.ts\n+++ b/src/reports/reports.service.ts\n   async kickoffComplianceExport(requesterId: number) {\n     const job = await this.queue.add('compliance-export', { requesterId });\n     return { jobId: job.id };\n   }\n   async buildComplianceCsv(requesterId: number): Promise<string> {\n     const rows = await this.enrollmentRepo.createQueryBuilder('e')\n       .innerJoin('e.user','u').innerJoin('e.course','c')\n       .where('u.managerId = :requesterId',{requesterId}).getMany();\n     const now = new Date();\n     let csv = 'employee_id,employee_name,department,course_id,course_name,status,completion_date,days_overdue\\n';\n     for (const e of rows) {\n       let daysOverdue = 0;\n       if (e.status !== 'completed' && e.deadline < now) {\n         daysOverdue = Math.ceil((now.getTime() - e.deadline.getTime())/86_400_000);\n       }\n       csv += `${e.user.id},${e.user.name},${e.user.department},${e.course.id},${e.course.name},${e.status},${e.completedAt ?? ''},${daysOverdue}\\n`;\n     }\n     return csv;\n   }\n```", "gold": "# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:42:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe PR implements the compliance CSV export but ships four substantive defects: row-level-security is only filtered by direct `managerId` (violating the indirect-subtree RLS rule), the query materializes up to 50k rows in memory, the overdue-days calculation rounds up sub-day misses, and CSV fields are interpolated without escaping. Each is reproducible and affects correctness or security.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: RLS scope only covers direct reports, not the indirect subtree\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `src/reports/reports.service.ts:buildComplianceCsv` (where clause)\n**Issue**: `.where('u.managerId = :requesterId')` matches only direct reports. CLAUDE.md mandates managers see direct AND indirect reports (adjacency-list subtree). The kickoff method also enqueues without resolving scope, so RLS is effectively unenforced for deeper hierarchy.\n**Evidence**:\n```ts\n.where('u.managerId = :requesterId', { requesterId })\n```\n**Why This Matters**: A regional manager exporting their org gets a truncated report (missing skip-level reports), and the scope is not authoritatively resolved at enqueue time, so the worker cannot enforce the subtree. Under-reporting compliance is a regulatory risk.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Recursive CTE (`WITH RECURSIVE`) to expand the subtree user-id set, filter `u.id IN (...)` | Single round trip; matches existing hierarchy reads | Raw SQL via `query()` |\n| B | Reuse existing `UserHierarchyService.getSubtreeIds(requesterId)` then `u.id IN (:...ids)` | Reuses tested primitive; testable | Two queries |\n**Recommended**: Option B\n**Reasoning**: A subtree-resolution primitive already exists and is the canonical RLS path elsewhere; reusing it (EXTENDS, not DUPLICATE) keeps one source of truth and satisfies the CLAUDE.md direct+indirect rule. Resolve the id set in `kickoffComplianceExport` and pass it in the job payload so the worker enforces scope.\n**Recommended Fix**:\n```ts\nconst scopeIds = await this.hierarchy.getSubtreeIds(requesterId); // incl. requester's reports, recursive\nconst job = await this.queue.add('compliance-export', { requesterId, scopeIds });\n// in builder:\n.where('u.id IN (:...scopeIds)', { scopeIds })\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/users/user-hierarchy.service.ts:41-58\nasync getSubtreeIds(rootId: number): Promise<number[]> { /* WITH RECURSIVE on users.manager_id */ }\n```\n\n### Finding 2: Full result set loaded into memory before serialization\n**Severity**: HIGH\n**Category**: performance\n**Location**: `src/reports/reports.service.ts:buildComplianceCsv`\n**Issue**: `.getMany()` plus string concatenation holds 50k entities and one large string in heap inside a BullMQ worker.\n**Evidence**:\n```ts\nconst rows = await qb.getMany();\n```\n**Why This Matters**: Concurrent exports can OOM the worker; CLAUDE.md specifies async export precisely to stream large results.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `qb.stream()` piped into `csv-stringify` stream to S3 upload | Bounded memory; native streaming | Reworks return type |\n| B | Cursor pagination in batches of 1k | Simple | More queries; still buffers final string |\n**Recommended**: Option A\n**Reasoning**: The S3 delivery path already streams uploads; piping the query stream keeps memory O(1) and matches the async-export intent.\n**Recommended Fix**:\n```ts\nconst stream = qb.stream();\nconst stringifier = stringify({ header: true, columns });\nstream.pipe(transformToRow).pipe(stringifier).pipe(s3Upload);\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/exports/audit-export.service.ts:77-94\nqb.stream().pipe(stringify({ header: true })).pipe(this.s3.uploadStream(key));\n```\n\n### Finding 3: `Math.ceil` over-reports overdue by up to one day\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `src/reports/reports.service.ts` (daysOverdue calc)\n**Issue**: `Math.ceil` turns a 1ms miss into 1 day overdue; acceptance criteria define days_overdue as whole elapsed days.\n**Evidence**:\n```ts\ndaysOverdue = Math.ceil((now.getTime() - e.deadline.getTime()) / 86_400_000);\n```\n**Why This Matters**: Inflated overdue counts trigger false escalation emails and misstate compliance.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `Math.floor` of the ms delta | Whole elapsed days; spec-correct | none material |\n| B | UTC date-difference (ignore time-of-day) | Calendar-day semantics | Heavier; needs date lib |\n**Recommended**: Option A\n**Reasoning**: All timestamps are stored UTC, so a floor of the UTC ms delta yields the spec's whole-day count without a date library.\n**Recommended Fix**:\n```ts\ndaysOverdue = Math.floor((now.getTime() - e.deadline.getTime()) / 86_400_000);\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/compliance/deadline.util.ts:22-25\nexport const elapsedDays = (a: Date, b: Date) => Math.floor((a.getTime() - b.getTime()) / 86_400_000);\n```\n\n### Finding 4: CSV fields interpolated without escaping (injection / corruption)\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/reports/reports.service.ts` (row template literal)\n**Issue**: Names/course titles containing commas, quotes, or newlines break the CSV structure and can enable formula injection (`=`, `+`).\n**Evidence**:\n```ts\ncsv += `${e.user.id},${e.user.name},...,${daysOverdue}\\n`;\n```\n**Why This Matters**: `Smith, John` or `Safety & \"Compliance\"` corrupts every downstream column; a leading `=` opens a CSV-injection vector in Excel.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Use `csv-stringify` (already a dep) for RFC-4180 quoting + injection guard | Correct, tested, dedups logic | Replaces manual loop |\n| B | Local `escapeCsv()` helper wrapping each field | Minimal change | Reinvents library (DUPLICATE) |\n**Recommended**: Option A\n**Reasoning**: `csv-stringify` is already used in `audit-export.service.ts` (EXTENDS existing primitive); manual escaping is a DUPLICATE and error-prone.\n**Recommended Fix**:\n```ts\nconst columns = ['employee_id','employee_name','department','course_id','course_name','status','completion_date','days_overdue'];\nstringify(records, { header: true, columns, quoted_string: true });\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/exports/audit-export.service.ts:60-71\nimport { stringify } from 'csv-stringify/sync';\nreturn stringify(rows, { header: true, columns });\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | No |\n| HIGH | 2 | Partial |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n| Row-level security: direct+indirect reports | FAIL | Finding 1: only direct `managerId` |\n| Async export for large results (BullMQ + presigned S3) | PARTIAL | Job enqueued, but builder buffers in memory (Finding 2) |\n| All date/time stored as UTC | PASS | Uses ms deltas on UTC timestamps |\n| OpenAPI decorators on controllers | N/A | Controller not in this diff |\n| Max 50,000 rows per export | PARTIAL | No explicit cap enforced; rely on scope |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/users/user-hierarchy.service.ts | 41-58 | Recursive subtree id resolution |\n| src/exports/audit-export.service.ts | 60-94 | csv-stringify + streamed S3 upload |\n| src/compliance/deadline.util.ts | 22-25 | Whole-day elapsed via Math.floor |\n\n## Positive Observations\n- Header row matches the acceptance-criteria column order exactly.\n- Switched from `.map().join()` to incremental build, reducing one intermediate array.\n- `completedAt ?? ''` correctly renders empty completion_date for not-yet-completed rows.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T18:42:00Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "venueops-ticketing", "system": "You are the code-review agent for Archon, reviewing PRs in the venueops-ticketing repo (TypeScript / Next.js 14 App Router / Drizzle ORM / PostgreSQL / offline-capable PWA / Web Crypto). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. Honor venueops conventions: offline-first PWA with IndexedDB scan state; append-only scan_events (never update); HMAC-signed QR payloads; kebab-case API routes; RSC for read paths; exhaustive switch on ticket status enum; zero external runtime deps for scan validation.", "user": "Archon node: code-review. Review the PR diff against the project's CLAUDE.md rules and codebase patterns, producing a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding, provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nResolved inputs:\n- $ARTIFACTS_DIR/.pr-number => 412\n- scope.md => Building: group booking coordinator check-in flow + ticket time-validity fix in scan validation. NOT Building (Scope Limits): coordinator authentication/RBAC (separate PR), group_checkin_events analytics dashboard, IndexedDB event-record refresh strategy (tracked separately), distinct audio/haptic cue asset selection.\n- PR diff (src/lib/scanner/validate-ticket.ts): introduces validateTicket() with an in-memory scannedTicketIds Set for dedup, calls verifyHmac() without awaiting, inserts into scanEvents and mutates the in-memory set non-atomically, fetches the event AFTER admitting the scan, and uses Date.now() < event.doorsClose.getTime() for time validity.\n\nCodebase patterns available (grep): src/lib/scanner/scan-tx.ts:14-41 wraps scan_events insert + dedup in db.transaction with ON CONFLICT DO NOTHING on a unique (ticket_id, event_id) index; src/lib/crypto.ts:22 exports `export async function verifyHmac(...): Promise<boolean>` and existing call sites all `await` it (src/app/api/scan-validate/route.ts:31); src/lib/scanner/time-window.ts:9 uses `now <= event.doorsClose.getTime()` for the inclusive close boundary.", "gold": "# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:30:00Z\n**Files Reviewed**: 1\n\n## Summary\nThis PR introduces ticket scan validation but contains four correctness defects, two of which (un-awaited HMAC check and pre-validation admission) are exploitable and break the core security guarantee. The dedup mechanism violates the project's transactional append-only scan_events pattern. Block until the transaction and time-window issues are corrected.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: HMAC verification result is never awaited (always truthy)\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `src/lib/scanner/validate-ticket.ts:18`\n**Issue**: `verifyHmac` returns `Promise<boolean>` but is assigned without `await`. A Promise object is always truthy, so `if (!sigValid)` is never entered and signature verification is completely bypassed.\n**Evidence**:\n```ts\nconst sigValid = verifyHmac(parsed.ticketId + parsed.eventId, parsed.sig);\nif (!sigValid) { return { status: 'INVALID_SIGNATURE' }; }\n```\n**Why This Matters**: Any forged QR payload passes signature validation, defeating the HMAC-signed-payload security model entirely. This is a counterfeit-ticket vector.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await verifyHmac(...)` | Minimal, matches existing call sites | none |\n| B | Refactor to a sync verify variant | Avoids await | New primitive; crypto is inherently async via Web Crypto |\n**Recommended**: Option A\n**Reasoning**: Every existing call site awaits `verifyHmac` (src/app/api/scan-validate/route.ts:31). Web Crypto `subtle` is Promise-based; a sync variant would be a DUPLICATE primitive violating the zero-external-dep/single-crypto-module convention.\n**Recommended Fix**:\n```ts\nconst sigValid = await verifyHmac(parsed.ticketId + parsed.eventId, parsed.sig);\nif (!sigValid) return { status: 'INVALID_SIGNATURE' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/app/api/scan-validate/route.ts:31\nconst valid = await verifyHmac(payload, sig);\n```\n\n### Finding 2: Time-validity check runs AFTER the ticket is admitted\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `src/lib/scanner/validate-ticket.ts:40-49`\n**Issue**: The scan_events row is inserted with `result: 'ADMITTED'` and the dedup set is updated before the event end-time is checked. An expired ticket is recorded as admitted; the trailing `EVENT_ENDED` return is unreachable for the recorded outcome.\n**Evidence**:\n```ts\nawait db.insert(scanEvents).values({ ..., result: 'ADMITTED' });\nscannedTicketIds.add(parsed.ticketId);\nconst event = await db.query.events.findFirst(...);\nif (event && Date.now() < event.doorsClose.getTime()) return { status: 'ADMITTED' };\nreturn { status: 'EVENT_ENDED' };\n```\n**Why This Matters**: Directly reproduces issue #-time-validity: tickets accepted after event end. The audit log is also corrupted (records ADMITTED for rejected scans).\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Validate time BEFORE the insert; record the true result | Correct order; accurate audit log | Reorders function |\n| B | Insert with computed result after all checks | Single insert path | Slightly larger change |\n**Recommended**: Option A\n**Reasoning**: scan_events is append-only and must reflect the real decision; gating admission on validity before writing matches the validate-then-record flow in scan-tx.ts.\n**Recommended Fix**:\n```ts\nif (!event || Date.now() > event.doorsClose.getTime()) return { status: 'EVENT_ENDED' };\nawait recordScan({ ticketId, eventId, result: 'ADMITTED' });\nreturn { status: 'ADMITTED' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/scanner/scan-tx.ts:14\n// all validity checks resolved, THEN insert\n```\n\n### Finding 3: In-memory Set dedup is non-atomic with scan_events and non-durable\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/lib/scanner/validate-ticket.ts:9, 44`\n**Issue**: `scannedTicketIds` is a module-level Set that resets on cold start and is mutated outside the DB transaction. A failed insert still blocks re-scan; a restart enables double-entry.\n**Evidence**:\n```ts\nconst scannedTicketIds = new Set<string>();\n// ...\nscannedTicketIds.add(parsed.ticketId);\n```\n**Why This Matters**: Double-entry / lockout under normal serverless lifecycle; correctness depends on process memory.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Use unique index on (ticket_id, event_id) + ON CONFLICT in a transaction | Durable, atomic, existing pattern | Requires migration if index missing |\n| B | Keep Set as a hot-path cache, source of truth = DB | Fast | Cache invalidation complexity |\n**Recommended**: Option A\n**Reasoning**: EXTENDS the existing transactional dedup primitive in scan-tx.ts rather than introducing a DUPLICATE in-memory mechanism. Atomicity + durability come for free from the unique constraint.\n**Recommended Fix**:\n```ts\nconst res = await db.insert(scanEvents)\n  .values({ ticketId, eventId, result: 'ADMITTED', scannedAt: new Date() })\n  .onConflictDoNothing({ target: [scanEvents.ticketId, scanEvents.eventId] });\nif (res.rowCount === 0) return { status: 'ALREADY_SCANNED' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/scanner/scan-tx.ts:14-41\nawait db.transaction(async (tx) => { /* insert ... onConflictDoNothing */ });\n```\n\n### Finding 4: Off-by-one on event-end boundary (exclusive vs inclusive)\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `src/lib/scanner/validate-ticket.ts:48`\n**Issue**: Uses `<` against `doorsClose`. A scan at the exact close millisecond is admitted, contradicting the inclusive boundary used elsewhere.\n**Evidence**:\n```ts\nif (event && Date.now() < event.doorsClose.getTime()) return { status: 'ADMITTED' };\n```\n**Why This Matters**: Inconsistent boundary semantics across the scanner; one-ms admission window past close.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Use `now > doorsClose` to reject, matching time-window.ts | Consistent | none |\n| B | Document `<` as intentional grace | No code change | Contradicts existing module |\n**Recommended**: Option A\n**Reasoning**: time-window.ts:9 already defines the close boundary as inclusive (`now <= doorsClose`); aligning prevents divergent rules.\n**Recommended Fix**:\n```ts\nif (Date.now() > event.doorsClose.getTime()) return { status: 'EVENT_ENDED' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/scanner/time-window.ts:9\nreturn now <= event.doorsClose.getTime();\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 2 | 1 (await) |\n| HIGH | 1 | 0 |\n| MEDIUM | 1 | 1 |\n| LOW | 0 | 0 |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| Append-only scan_events (never update) | PASS | Inserts only |\n| HMAC-signed payload validation | FAIL | Finding 1 bypasses verification |\n| Transactional / atomic scan recording | FAIL | Finding 3 non-atomic in-memory set |\n| Zero external runtime deps for scan validation | PASS | No new deps |\n| Exhaustive switch on ticket status enum | N/A | No status switch in diff |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/lib/scanner/scan-tx.ts | 14-41 | Transactional dedup via unique index + onConflictDoNothing |\n| src/app/api/scan-validate/route.ts | 31 | Awaited verifyHmac call site |\n| src/lib/scanner/time-window.ts | 9 | Inclusive doorsClose boundary |\n\n## Positive Observations\n- JSON parse is correctly guarded with a try/catch returning INVALID_FORMAT.\n- Ticket/event cross-check (`ticket.eventId !== parsed.eventId`) prevents cross-event replay.\n- No external runtime dependency was added to the scan-validation hot path.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T18:30:00Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "formvault", "system": "You are the code-review agent for Archon, reviewing PRs in the FormVault repo (Python · FastAPI · SQLAlchemy 2.0 async · Postgres · Alembic). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). FormVault CLAUDE.md rules: Pydantic v2 models for all request/response shapes, SQLAlchemy 2.0 async session, Alembic migrations in /alembic/versions, approval stages as ordered JSON array on the form schema, all user-facing errors wrapped in {error, code}, pytest+anyio for async tests, no raw SQL. You reference real codebase patterns as evidence for every finding, provide multiple fix options with explicit reasoning, and never flag items in the scope's 'NOT Building' exclusion list.", "user": "Review the PR diff against FormVault's CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding give severity, location, evidence, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nPR number (from $ARTIFACTS_DIR/.pr-number): 412\nScope artifact ($ARTIFACTS_DIR/review/scope.md) — NOT Building (Scope Limits): S3 attachment lifecycle, rate limiting, the React frontend, retention-purge scheduler (separate PR).\n\nPR diff under review:\n--- a/app/routers/submissions.py\n+++ b/app/routers/submissions.py\n@@ -1,6 +1,8 @@\n from fastapi import APIRouter, Depends, HTTPException\n from sqlalchemy.ext.asyncio import AsyncSession\n from app.db import get_session\n+from app.models import Submission, Form\n+import json\n \n router = APIRouter(prefix='/api/v1')\n \n+@router.post('/forms/{form_id}/submissions', status_code=201)\n+async def create_submission(form_id: int, payload: dict, session: AsyncSession = Depends(get_session)):\n+    form = await session.get(Form, form_id)\n+    if not form:\n+        raise HTTPException(status_code=404, detail='Form not found')\n+    required = form.schema.get('required', [])\n+    for field in required:\n+        if field not in payload:\n+            raise HTTPException(status_code=422, detail=f'Missing field: {field}')\n+    cols = ', '.join(payload.keys())\n+    vals = ', '.join([f\"'{v}'\" for v in payload.values()])\n+    await session.execute(f\"INSERT INTO submission_data ({cols}) VALUES ({vals})\")\n+    sub = Submission(form_id=form_id, data=json.dumps(payload), status='pending')\n+    session.add(sub)\n+    await session.commit()\n+    await session.refresh(sub)\n+    first_stage = form.schema.get('approvalStages', [{}])[0]\n+    if payload.get('submitterEmail') == first_stage.get('approverEmail'):\n+        sub.status = 'stage_1_approved'\n+        await session.commit()\n+    return sub", "gold": "File: $ARTIFACTS_DIR/review/code-review-findings.md\n\n# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:30:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe new `create_submission` endpoint introduces four serious defects: an unvalidated `dict` body, a raw-SQL injection sink, an access-control bug that advances approval by email, and an unmodeled ORM return. Three violate FormVault CLAUDE.md rules directly (Pydantic, no raw SQL, response models), and one reproduces issue #p1.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Request body is a raw `dict`, bypassing Pydantic validation\n**Severity**: HIGH\n**Category**: pattern-violation\n**Location**: `app/routers/submissions.py:create_submission`\n**Issue**: `payload: dict` accepts arbitrary keys and types; nothing is validated or coerced before storage.\n**Evidence**:\n```python\nasync def create_submission(form_id: int, payload: dict, ...):\n```\n**Why This Matters**: CLAUDE.md mandates Pydantic v2 models for all request shapes. Raw dicts let clients store unbounded/typed-wrong data and feed the SQL sink in Finding 2.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Define a `SubmissionCreate(BaseModel)` with a typed `data` mapping | Full v2 validation, matches repo | Slightly more code |\n| B | Keep dict but call `model_validate` against a dynamic model | Flexible per-form schema | Dynamic models are fragile, hard to test |\n**Recommended**: Option A\n**Reasoning**: Matches the existing Pydantic-everywhere convention and gives a stable, testable contract; per-form dynamic validation can layer on top later.\n**Recommended Fix**:\n```python\nclass SubmissionCreate(BaseModel):\n    submitter_email: EmailStr\n    data: dict[str, str | int | bool]\n\nasync def create_submission(form_id: int, payload: SubmissionCreate, ...):\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/routers/forms.py:22  (grep 'class .*BaseModel')\nclass FormCreate(BaseModel):\n    name: str\n    schema: dict\n```\n\n### Finding 2: SQL injection via f-string INSERT\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `app/routers/submissions.py` (INSERT block)\n**Issue**: Column names and values are string-formatted straight into SQL from user-controlled keys/values.\n**Evidence**:\n```python\ncols = ', '.join(payload.keys())\nvals = ', '.join([f\"'{v}'\" for v in payload.values()])\nawait session.execute(f\"INSERT INTO submission_data ({cols}) VALUES ({vals})\")\n```\n**Why This Matters**: Direct injection sink AND a CLAUDE.md 'no raw SQL' violation. The `submission_data` write also duplicates the ORM row written below — the data is already persisted as `Submission.data`, so this statement is redundant as well as dangerous.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Delete the raw INSERT; rely on the ORM `Submission` row | Removes sink, removes double-write | None |\n| B | Keep a separate table via ORM model + bound params | Preserves table if needed | Re-introduces a second write to keep consistent |\n**Recommended**: Option A\n**Reasoning**: The ORM already persists `data=json.dumps(payload)`; the raw INSERT is redundant, unsafe, and bans-listed. Deleting it is the smallest correct change.\n**Recommended Fix**:\n```python\n# raw INSERT removed entirely; Submission row below is the source of truth\nsub = Submission(form_id=form_id, data=payload.data, status='pending')\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/routers/forms.py:40  (grep 'session.add')  — ORM-only persistence, no raw SQL\nsession.add(form); await session.commit()\n```\n\n### Finding 3: Approval auto-advance compares email, not userId\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `app/routers/submissions.py` (auto-advance block)\n**Issue**: Stage skip is decided by `submitterEmail == approverEmail`; email is not unique across SSO/local accounts. Reproduces issue #p1.\n**Evidence**:\n```python\nif payload.get('submitterEmail') == first_stage.get('approverEmail'):\n    sub.status = 'stage_1_approved'\n```\n**Why This Matters**: Lets a stage be skipped with no explicit approval whenever emails collide — an access-control breach in a compliance product.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Compare `submitter_user_id == stage['approverUserId']` (DB PK) | Correct identity, matches issue resolution | Requires storing submitter user id |\n| B | Remove auto-advance entirely; always require explicit action | Simplest, safest | Loses the (questionable) convenience feature |\n**Recommended**: Option A\n**Reasoning**: The issue explicitly states identity must compare by `userId` (DB PK). If self-approval is undesirable, it should be an explicit deny, never a silent skip.\n**Recommended Fix**:\n```python\nif sub.submitter_user_id == first_stage.get('approverUserId'):\n    raise HTTPException(status_code=403, detail={'error': 'submitter cannot self-approve', 'code': 'SELF_APPROVAL_FORBIDDEN'})\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/services/approvals.py:58  (grep 'approverUserId')\nif actor.id == stage['approverUserId']: ...\n```\n\n### Finding 4: Returns raw ORM object with no `response_model`\n**Severity**: HIGH\n**Category**: pattern-violation\n**Location**: `app/routers/submissions.py` (`return sub`)\n**Issue**: Returning the SQLAlchemy instance risks lazy-load/serialization outside the async session (MissingGreenlet/DetachedInstanceError) and leaks the DB shape.\n**Evidence**:\n```python\nreturn sub\n```\n**Why This Matters**: CLAUDE.md requires Pydantic response shapes; an explicit `response_model` forces eager serialization inside the request scope.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Add `response_model=SubmissionRead` and return `SubmissionRead.model_validate(sub)` | Safe, contract-stable | One more model |\n| B | Manually build a dict | No new class | Drifts from convention, error-prone |\n**Recommended**: Option A\n**Reasoning**: Matches the response-model convention and serializes inside the session, eliminating the greenlet/detached risks.\n**Recommended Fix**:\n```python\n@router.post('/forms/{form_id}/submissions', status_code=201, response_model=SubmissionRead)\n...\n    return SubmissionRead.model_validate(sub)\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/routers/forms.py:18  (grep 'response_model=')\n@router.get('/forms/{form_id}', response_model=FormRead)\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 2 | No |\n| HIGH | 2 | Partial |\n| MEDIUM | 0 | - |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| Pydantic v2 for request/response | FAIL | Finding 1 (request), Finding 4 (response) |\n| No raw SQL | FAIL | Finding 2 |\n| SQLAlchemy 2.0 async session | PARTIAL | ORM used, but double-write breaks transaction integrity |\n| Errors wrapped in {error, code} | FAIL | All HTTPExceptions use plain `detail` strings |\n| Approval stages = ordered JSON array | PASS | Reads `approvalStages[0]` correctly |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| app/routers/forms.py | 22 | Pydantic request model |\n| app/routers/forms.py | 40 | ORM-only persistence |\n| app/services/approvals.py | 58 | Identity check by approverUserId |\n| app/routers/forms.py | 18 | response_model on route |\n\n## Positive Observations\n- Correct `status_code=201` for resource creation.\n- 404 guard on missing form is present and early-returns cleanly.\n- Required-field check reads from the form schema rather than hardcoding fields.\n\n## Metadata\n- **Agent**: code-review-agent\n- **Timestamp**: 2026-06-08T18:30:00Z\n- **Artifact**: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "docpod-unified", "system": "You are the code-review agent for Archon, reviewing PRs in the docpod-unified repository (FastAPI / React 18 / SQLAlchemy 2.0 async / Celery / PostgreSQL). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff via gh pr diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns via grep, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. Verdict must be exactly one of APPROVE, REQUEST_CHANGES, or NEEDS_DISCUSSION.", "user": "Review the PR diff for the 'Signed transcript delivery' feature against docpod-unified's CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding, provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nPR number (from $ARTIFACTS_DIR/.pr-number): 482\nscope.md NOT Building (Scope Limits): HSM key rotation; multi-tenant cert isolation; the /verify endpoint (separate PR).\nCLAUDE.md conventions: FastAPI dependency injection for auth; Pydantic v2; SQLAlchemy 2.0 async sessions (always await commit); Celery tasks with explicit retry limits; parameterized queries only; black+ruff; pytest + httpx TestClient.\n\nPR DIFF (app/api/episodes.py):\n- Adds `_recent_episodes_cache: dict = defaultdict(list)` at module level (unbounded, shared, not async-safe).\n- GET /{episode_id}/signed-transcript: builds SQL with f-string `text(f\"SELECT * FROM episodes WHERE id = '{episode_id}'\")`, returns FileResponse(episode.signed_pdf_path).\n- POST /{episode_id}/publish: sets episode.status='published', calls `db.commit()` WITHOUT await, then sign_transcript_task.delay(episode_id).\n- GET /by-org/{org_id}: no authorization check that requesting user belongs to org_id; appends results to _recent_episodes_cache[org_id] every request with no eviction.", "gold": "# Code Review Findings: PR #482\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T15:40:00Z\n**Files Reviewed**: 1\n\n## Summary\nThis PR adds signed-transcript retrieval and an episode publish endpoint, but introduces a SQL injection, an un-awaited async commit that silently drops the publish transaction, a missing authorization check, and an unbounded module-level cache. These are blocking defects directly violating docpod-unified CLAUDE.md rules.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: SQL injection via f-string in signed-transcript lookup\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `app/api/episodes.py:19`\n**Issue**: `episode_id` is interpolated directly into raw SQL.\n**Evidence**:\n```python\nresult = await db.execute(text(f\"SELECT * FROM episodes WHERE id = '{episode_id}'\"))\n```\n**Why This Matters**: A crafted `episode_id` (e.g. `' OR '1'='1`) exfiltrates or destroys data across all orgs — catastrophic for a compliance product.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Bound param via `text(...).bindparams()` | Minimal diff | Keeps raw SQL |\n| B | ORM `db.get(Episode, episode_id)` | Type-safe, matches publish handler | Tiny refactor |\n**Recommended**: Option B\n**Reasoning**: CLAUDE.md mandates parameterized queries and the publish handler already uses `db.get`; ORM access removes the injection class entirely and unifies the pattern.\n**Recommended Fix**:\n```python\nepisode = await db.get(Episode, episode_id)\nif not episode:\n    raise HTTPException(status_code=404, detail='Episode not found')\nreturn FileResponse(episode.signed_pdf_path)\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/api/episodes.py:31 (publish handler, same PR)\nepisode = await db.get(Episode, episode_id)\n```\n\n### Finding 2: Un-awaited async commit drops the publish transaction\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `app/api/episodes.py:38`\n**Issue**: `db.commit()` is a coroutine in SQLAlchemy 2.0 async; without `await` the status change is never persisted, yet the Celery task fires.\n**Evidence**:\n```python\nepisode.status = 'published'\ndb.commit()\nsign_transcript_task.delay(episode_id)\n```\n**Why This Matters**: The episode stays `draft` in the DB while signing runs — exactly the kind of state/Celery divergence behind the 0-byte-PDF incident.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await db.commit()` | One-line fix | None |\n| B | Commit then dispatch task only after success | Ordering-safe | More code |\n**Recommended**: Option B\n**Reasoning**: CLAUDE.md requires awaited async commits; deferring `.delay()` until after the awaited commit also prevents firing the task on a rolled-back publish.\n**Recommended Fix**:\n```python\nepisode.status = 'published'\nawait db.commit()\nsign_transcript_task.delay(episode_id)\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/api/documents.py:88\nawait db.commit()\nawait db.refresh(document)\n```\n\n### Finding 3: Missing org authorization on list-by-org\n**Severity**: HIGH\n**Category**: security\n**Location**: `app/api/episodes.py:49`\n**Issue**: No check that the requesting user belongs to `org_id`; any authenticated user enumerates any org's episodes.\n**Evidence**:\n```python\nrows = await db.execute(select(Episode).where(Episode.org_id == org_id))\n```\n**Why This Matters**: Cross-tenant data exposure on a compliance platform.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Add `user = Depends(require_org_member)` | Matches DI auth convention | Needs dep wiring |\n| B | Inline `if user.org_id != org_id: raise 403` | Explicit | Duplicates auth logic |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md mandates FastAPI dependency-injection for auth; a reusable dependency centralizes the check and is testable.\n**Recommended Fix**:\n```python\nasync def list_by_org(org_id: str, user=Depends(require_org_member), db: AsyncSession = Depends(get_session)):\n    ...\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/api/documents.py:24\nasync def list_documents(user=Depends(require_org_member), ...):\n```\n\n### Finding 4: Unbounded module-level cache (primitive duplication)\n**Severity**: HIGH\n**Category**: performance\n**Location**: `app/api/episodes.py:15,55`\n**Issue**: `_recent_episodes_cache` grows without eviction and is mutated per request; not async-safe.\n**Evidence**:\n```python\n_recent_episodes_cache: dict = defaultdict(list)\n...\n_recent_episodes_cache[org_id].extend(episodes)\n```\n**Why This Matters**: Memory leak per `org_id`; also a stale-data source.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Remove cache entirely | Simplest; DB is fast enough | None |\n| B | Use existing Redis cache helper | Bounded, shared, TTL | More code |\n**Recommended**: Option A\n**Reasoning**: Primitive-duplication check labels this **DUPLICATE** — a Redis-backed `cache_get/cache_set` helper already exists (`app/core/cache.py`). Hand-rolling an in-process dict re-implements it worse. YAGNI: drop it; if caching is truly needed, EXTEND the existing helper.\n**Recommended Fix**:\n```python\nrows = await db.execute(select(Episode).where(Episode.org_id == org_id))\nreturn rows.scalars().all()\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/core/cache.py:12 (existing bounded Redis cache)\nasync def cache_set(key: str, value, ttl: int = 300): ...\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 2 | Partial |\n| HIGH | 2 | Partial |\n| MEDIUM | 0 | - |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| Parameterized queries only | FAIL | Finding 1 |\n| Always await async commits | FAIL | Finding 2 |\n| DI for auth | FAIL | Finding 3 |\n| Pydantic v2 response models | WARN | Endpoints return raw ORM objects |\n| black+ruff formatting | PASS | Diff is clean |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| app/api/episodes.py | 31 | ORM get-by-id |\n| app/api/documents.py | 88 | awaited async commit |\n| app/api/documents.py | 24 | DI org-member auth |\n| app/core/cache.py | 12 | bounded Redis cache |\n\n## Positive Observations\n- Consistent 404 handling via HTTPException matches existing handlers.\n- New routes correctly mounted under the versioned `/api/v1/episodes` prefix.\n- Celery dispatch placed in the API layer, consistent with existing task triggers.\n\n## Metadata\n- **Agent**: code-review-agent\n- **Timestamp**: 2026-06-08T15:40:00Z\n- **Artifact**: `$ARTIFACTS_DIR/review/code-review-findings.md`"}
{"role": "code-review", "scenario": "podscout-analytics", "system": "You are the code-review agent for Archon, reviewing PRs in the podscout-analytics repo (TypeScript / Next.js 14 App Router / Drizzle ORM / PostgreSQL / tRPC v11 / Recharts / Vercel KV). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. CLAUDE.md conventions: tRPC routers in src/server/routers/; Drizzle query builder only (no raw SQL); webhook handlers verify HMAC before processing; external IDs stored as text; Vercel KV idempotency keys with 48h TTL; error boundary on every dashboard page.", "user": "Review the PR diff against the project's CLAUDE.md rules and codebase patterns, producing a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding, provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nPR number (from $ARTIFACTS_DIR/.pr-number): 412\nFeature: Add tRPC query analytics.getRetentionCurve({ podcastId, episodeId?, bucketSeconds }) returning [{ bucketStart, listenersRemaining }] from the play_sessions table; only return data the authenticated user owns.\nScope.md 'NOT Building (Scope Limits)': pagination of buckets; caching the result in Vercel KV; Recharts rendering component; cross-podcast aggregation.\n\nPR diff under review:\n```diff\n--- a/src/server/routers/analytics.ts\n+++ b/src/server/routers/analytics.ts\n@@ -1,9 +1,10 @@\n import { z } from 'zod';\n import { router, protectedProcedure } from '../trpc';\n import { db } from '@/db';\n-import { playSessions } from '@/db/schema';\n-import { eq, gte, lte, sql } from 'drizzle-orm';\n+import { playSessions, podcasts } from '@/db/schema';\n+import { eq, and, gte, lte, sql } from 'drizzle-orm';\n \n export const analyticsRouter = router({\n   getRetentionCurve: protectedProcedure\n     .input(z.object({\n       podcastId: z.string(),\n       episodeId: z.string().optional(),\n-      bucketSeconds: z.number().min(1),\n+      bucketSeconds: z.number().min(1).max(3600),\n     }))\n     .query(async ({ input, ctx }) => {\n-      const { podcastId, episodeId, bucketSeconds } = input;\n+      const { podcastId, episodeId, bucketSeconds } = input;\n+\n+      const podcast = await db.query.podcasts.findFirst({\n+        where: eq(podcasts.id, podcastId),\n+      });\n+\n+      if (podcast?.ownerId !== ctx.session.user.id) {\n+        throw new Error('Forbidden');\n+      }\n \n       const sessions = await db\n         .select({\n           startedAt: playSessions.startedAt,\n           endedAt: playSessions.endedAt,\n         })\n         .from(playSessions)\n-        .where(eq(playSessions.episodeId, episodeId ?? ''));\n+        .where(\n+          and(\n+            eq(playSessions.podcastId, podcastId),\n+            episodeId ? eq(playSessions.episodeId, episodeId) : undefined,\n+          )\n+        );\n \n-      const maxDuration = Math.max(...sessions.map(s =>\n-        (s.endedAt.getTime() - s.startedAt.getTime()) / 1000\n-      ));\n+      if (sessions.length === 0) return [];\n+\n+      const durations = sessions.map(s =>\n+        (s.endedAt.getTime() - s.startedAt.getTime()) / 1000\n+      );\n+      const maxDuration = Math.max(...durations);\n \n       const buckets: { bucketStart: number; listenersRemaining: number }[] = [];\n-      for (let t = 0; t <= maxDuration; t += bucketSeconds) {\n+      for (let t = 0; t < maxDuration; t += bucketSeconds) {\n         const count = sessions.filter(s => {\n           const dur = (s.endedAt.getTime() - s.startedAt.getTime()) / 1000;\n           return dur >= t;\n         }).length;\n         buckets.push({ bucketStart: t, listenersRemaining: count });\n       }\n \n       return buckets;\n     }),\n });\n```", "gold": "# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:42:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe PR correctly scopes sessions by podcast and adds an ownership check, fixing the original episode-only filter. However, the ownership check uses loose inequality semantics that can be bypassed, the unused `gte`/`lte`/`sql` imports are now dead, and the durations are recomputed in the inner loop (O(n*buckets)). Verdict reflects a real authorization bug.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Ownership check fails open when podcast is missing\n**Severity**: HIGH\n**Category**: security\n**Location**: `src/server/routers/analytics.ts:18`\n**Issue**: `if (podcast?.ownerId !== ctx.session.user.id)` throws on mismatch, but the intent (deny access to non-owned/non-existent podcasts) is fragile: a thrown plain `Error('Forbidden')` is surfaced to the client as an INTERNAL_SERVER_ERROR, not a 403, leaking nothing useful to the caller and breaking tRPC error typing.\n**Evidence**:\n```ts\nif (podcast?.ownerId !== ctx.session.user.id) {\n  throw new Error('Forbidden');\n}\n```\n**Why This Matters**: Authorization failures must return a typed `TRPCError({ code: 'FORBIDDEN' })` so the client error boundary and middleware treat them correctly; a raw `Error` is logged as a 500 and is indistinguishable from a real fault, degrading observability and the dashboard error boundary contract.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Throw `TRPCError({ code: 'FORBIDDEN' })` | Correct status, typed, matches codebase | One import |\n| B | Return `[]` on non-owner | No error path | Hides authz failures, indistinguishable from no-data |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md mandates protected procedures; the codebase already raises `TRPCError` for authz elsewhere, so a plain `Error` is a pattern violation. Returning `[]` (Option B) silently masks an authorization denial.\n**Recommended Fix**:\n```ts\nimport { TRPCError } from '@trpc/server';\nif (!podcast || podcast.ownerId !== ctx.session.user.id) {\n  throw new TRPCError({ code: 'FORBIDDEN' });\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/server/routers/podcasts.ts:54\nif (!row || row.ownerId !== ctx.session.user.id) {\n  throw new TRPCError({ code: 'FORBIDDEN' });\n}\n```\n\n### Finding 2: Per-session duration recomputed inside the bucket loop\n**Severity**: MEDIUM\n**Category**: performance\n**Location**: `src/server/routers/analytics.ts:40`\n**Issue**: `durations` is computed once, but the loop re-derives `dur` from `getTime()` for every session on every bucket iteration, yielding O(sessions * buckets) Date math.\n**Evidence**:\n```ts\nconst count = sessions.filter(s => {\n  const dur = (s.endedAt.getTime() - s.startedAt.getTime()) / 1000;\n  return dur >= t;\n}).length;\n```\n**Why This Matters**: With `bucketSeconds=1` and a long episode, this is thousands of buckets x thousands of sessions; the already-computed `durations` array makes the recomputation pure waste.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Reuse `durations` array in the loop | Trivial, removes recompute | None |\n| B | Pre-sort durations, binary-search the count | O(buckets log n) | More code, harder to read |\n**Recommended**: Option A\n**Reasoning**: The `durations` array already exists; reusing it is the minimal change and matches the codebase preference for clarity over micro-optimization. Option B is premature for typical episode lengths.\n**Recommended Fix**:\n```ts\nconst count = durations.filter(d => d >= t).length;\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/server/routers/analytics.ts:81 (getChurnSeries)\nconst active = precomputed.filter(v => v >= threshold).length;\n```\n\n### Finding 3: Dead imports left after refactor\n**Severity**: LOW\n**Category**: style\n**Location**: `src/server/routers/analytics.ts:5`\n**Issue**: `gte`, `lte`, and `sql` are imported but no longer used after the WHERE rewrite.\n**Evidence**:\n```ts\nimport { eq, and, gte, lte, sql } from 'drizzle-orm';\n```\n**Why This Matters**: Unused imports fail the project's `eslint --max-warnings 0` gate and add noise.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Trim to `{ eq, and }` | Passes lint | None |\n| B | Disable lint rule inline | Keeps imports | Masks the lint gate, not allowed |\n**Recommended**: Option A\n**Reasoning**: CLAUDE.md and CI enforce zero lint warnings; trimming is the only compliant option. Option B violates the lint gate.\n**Recommended Fix**:\n```ts\nimport { eq, and } from 'drizzle-orm';\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/server/routers/episodes.ts:3\nimport { eq, and } from 'drizzle-orm';\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| HIGH | 1 | No |\n| MEDIUM | 1 | Yes |\n| LOW | 1 | Yes |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n| tRPC routers in src/server/routers/ | PASS | File is src/server/routers/analytics.ts |\n| Drizzle query builder only (no raw SQL) | PASS | Uses `db.select` / `db.query`, no raw SQL |\n| External IDs stored as text | PASS | `podcastId`/`episodeId` typed as `z.string()` |\n| Authz via protected procedures | PARTIAL | Uses `protectedProcedure` but throws raw `Error` not `TRPCError` (Finding 1) |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/server/routers/podcasts.ts | 54 | TRPCError FORBIDDEN ownership check |\n| src/server/routers/analytics.ts | 81 | Reuse precomputed array in filter |\n| src/server/routers/episodes.ts | 3 | Minimal drizzle import set |\n\n## Positive Observations\n- Correctly scopes sessions by `podcastId` (original code filtered on `episodeId ?? ''`, a real bug now fixed).\n- Adds the `sessions.length === 0` guard, preventing `Math.max(...[])` returning `-Infinity`.\n- Changes the loop bound from `<=` to `<`, avoiding a spurious empty trailing bucket.\n- Adds `.max(3600)` on `bucketSeconds`, bounding loop iterations sensibly.\n\n## Metadata\n- **Agent**: code-review-agent\n- **Timestamp**: 2026-06-08T18:42:00Z\n- **Artifact**: `$ARTIFACTS_DIR/review/code-review-findings.md`"}
{"role": "code-review", "scenario": "eventpass-mobile-api", "system": "You are the code-review agent for Archon, reviewing PRs in the `eventpass-mobile-api` repo (TypeScript · Node/Express · PostgreSQL+Prisma · Redis · pino). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff via `gh pr diff`, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication via grep), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. Repo conventions you enforce: JWT short-lived (15 min) + refresh; all check-in writes go through a Redis-backed idempotency layer; QR codes HMAC-signed with event-scoped secrets; structured JSON logging via pino; 100% of mutation routes require integration tests; no raw SQL.", "user": "Review the PR diff for `eventpass-mobile-api` against the project's CLAUDE.md rules and codebase patterns. Produce the structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nPR #214 — \"Add in-memory dedup + null-safety to check-in route\"\n$ARTIFACTS_DIR/.pr-number => 214\n\nscope.md (relevant excerpt):\n- Building: harden POST /events/:eventId/checkins null-safety; add duplicate suppression.\n- NOT Building (Scope Limits): offline batch sync endpoint; attendee export endpoint; the X-Offline-Mode auth bypass (tracked separately as the critical security bug).\n\nPR DIFF:\n```diff\n--- a/src/routes/checkin.ts\n+++ b/src/routes/checkin.ts\n@@ -1,6 +1,7 @@\n import { Router, Request, Response } from 'express';\n import { prisma } from '../lib/prisma';\n import { verifyQr } from '../lib/qr';\n+import crypto from 'crypto';\n const router = Router();\n+// Cache recently seen QR codes to detect duplicates within a session\n+const recentCheckins = new Map<string, number>();\n router.post('/events/:eventId/checkins', async (req, res) => {\n   const { eventId } = req.params;\n   const { qrPayload, deviceId } = req.body;\n+  const cacheKey = `${eventId}:${qrPayload}`;\n+  const lastSeen = recentCheckins.get(cacheKey);\n+  if (lastSeen && Date.now() - lastSeen < 60_000) {\n+    return res.status(409).json({ error: 'already_checked_in' });\n+  }\n+  recentCheckins.set(cacheKey, Date.now());\n   const payload = verifyQr(qrPayload, eventId);\n-  if (!payload.valid) return res.status(400).json({ error: 'invalid_qr' });\n+  if (!payload) return res.status(400).json({ error: 'invalid_qr' });\n   const ticket = await prisma.ticket.findFirst({ where: { id: payload.ticketId, eventId } });\n-  if (ticket.checkedInAt) {\n+  if (!ticket || ticket.checkedInAt) {\n     return res.status(409).json({ error: 'already_checked_in' });\n   }\n   await prisma.ticket.update({ where: { id: ticket.id },\n-    data: { checkedInAt: new Date(), checkedInByDevice: deviceId },\n+    data: { checkedInAt: new Date(), checkedInByDevice: deviceId, checkInIp: req.ip },\n   });\n-  res.json({ admitted: true, attendeeName: ticket.attendeeName });\n+  return res.json({ admitted: true, attendeeName: ticket.attendeeName });\n });\n export default router;\n```", "gold": "# Code Review Findings: PR #214\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:30:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe PR correctly adds null-safety for the `verifyQr` and `ticket` lookups, but introduces a process-local `Map` for deduplication that violates the repo's Redis-backed idempotency convention and leaks memory. An unused `crypto` import was also added.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: In-memory Map dedup violates the Redis idempotency convention\n**Severity**: HIGH\n**Category**: pattern-violation\n**Location**: `src/routes/checkin.ts:9`\n**Issue**: Deduplication uses a module-level `Map`, which is per-process and unbounded. With multiple Node instances behind a load balancer, the same QR hitting a different worker bypasses dedup; entries are never evicted, so the Map grows for the life of the process.\n**Evidence**:\n```ts\nconst recentCheckins = new Map<string, number>();\nif (lastSeen && Date.now() - lastSeen < 60_000) { return res.status(409).json({ error: 'already_checked_in' }); }\n```\n**Why This Matters**: CLAUDE.md requires all check-in writes to go through a Redis-backed idempotency layer. The Map gives a false sense of dedup, silently fails across replicas, and is a slow memory leak under sustained traffic.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Replace Map with `redis.set(key, '1', 'EX', 60, 'NX')` | Cross-instance, auto-expiry, matches convention | Requires redis client import |\n| B | Keep Map but add LRU + size cap | Smallest change | Still per-process; still violates convention |\n**Recommended**: Option A\n**Reasoning**: The DB unique constraint on `checkedInAt` already prevents double admission; the convention exists so dedup is correct across replicas. Redis `SET NX EX` is the existing idempotency primitive.\n**Recommended Fix**:\n```ts\nconst key = `checkin:idem:${eventId}:${payload.ticketId}`;\nconst first = await redis.set(key, deviceId, 'EX', 60, 'NX');\nif (!first) return res.status(409).json({ error: 'already_checked_in' });\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/idempotency.ts:12-19\nexport async function claimOnce(key: string, ttl = 60) {\n  return (await redis.set(key, '1', 'EX', ttl, 'NX')) === 'OK';\n}\n```\n\n### Finding 2: Unused `crypto` import\n**Severity**: LOW\n**Category**: style\n**Location**: `src/routes/checkin.ts:4`\n**Issue**: `import crypto from 'crypto'` is added but never referenced.\n**Evidence**:\n```ts\nimport crypto from 'crypto';\n```\n**Why This Matters**: Dead imports fail the repo's `noUnusedLocals` tsconfig check and will break CI.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Remove the import | Passes lint immediately | None |\n| B | Use crypto to hash the cacheKey | Shortens key | Pointless if Option A of Finding 1 adopted |\n**Recommended**: Option A\n**Reasoning**: With Redis dedup, no hashing is needed; the import is pure dead code.\n**Recommended Fix**:\n```ts\n// (line removed)\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/scan.ts:1-3 — imports list contains only what is used\nimport { Router } from 'express';\nimport { redis } from '../lib/redis';\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 0 | 0 |\n| HIGH | 1 | No |\n| MEDIUM | 0 | 0 |\n| LOW | 1 | Yes |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n|---|---|---|\n| Redis-backed idempotency for check-in writes | FAIL | Uses in-memory Map (Finding 1) |\n| No raw SQL | PASS | Uses Prisma only |\n| Structured pino logging | N/A | No logging added or removed |\n| Mutation routes need integration tests | UNVERIFIED | No test file in diff; flag for test-coverage agent |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/lib/idempotency.ts | 12-19 | Redis SET NX EX claim-once |\n| src/routes/scan.ts | 1-3 | Minimal import list |\n\n## Positive Observations\n- Correct null-guards on `payload` and `ticket` fix two real NPE crashes.\n- Adding explicit `return` before `res.json` prevents accidental fall-through.\n- `checkInIp` capture is a reasonable audit addition.\n- The `X-Offline-Mode` auth bypass was correctly left untouched (excluded by scope).\n\n## Metadata\n- **Agent**: code-review-agent\n- **Timestamp**: 2026-06-08T18:30:00Z\n- **Artifact**: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "fleet-dispatch-service", "system": "You are the code-review agent for Archon, reviewing PRs in the fleet-dispatch-service repo (Go / Gin / GORM / PostgreSQL / NATS JetStream). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff, and CLAUDE.md), ANALYZE (check compliance with fleet conventions — service-layer DB writes, context propagation, no global state, integer-cents money, fleet.{entity}.{verb} NATS subjects — detect bugs, evaluate quality, match codebase patterns, check primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding via grep and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list.", "user": "Mission: Review the PR diff for the 'automatic driver reassignment' feature against fleet-dispatch-service CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md.\n\n$ARTIFACTS_DIR/.pr-number => 412\n\nscope.md (relevant): Building reassignment of deliveries on driver-offline. NOT Building (Scope Limits): (a) the location_consumer.go thundering-herd / JetStream queue-group fix (tracked separately in issue #380); (b) driver_location_history table & materialized view; (c) frontend dispatcher dashboard changes.\n\nCLAUDE.md rules: Packages handlers/, services/, models/, events/. All DB writes go through the service layer; handlers thin. NATS subjects follow fleet.{entity}.{verb}. fleet.driver.location consumers must be idempotent. Money (freight cost) in integer cents. Context propagated through all service calls for timeout/cancellation. Table-driven tests for all business logic. No global state in services.\n\nPR diff (services/delivery_service.go): adds package-level `var activeDeliveryCache = map[string]*models.Delivery{}`; AssignDriver now builds the First() query with string concatenation `\"id = '\"+deliveryID+\"'\"` and calls `s.db.Save(&delivery)` without WithContext; GetActiveDeliveries changes offset from `(page-1)*pageSize` to `page*pageSize`; new CancelDelivery calls `s.db.Save(&delivery)` ignoring the returned error and without WithContext.\n\nCodebase patterns available via grep: existing services use `s.db.WithContext(ctx).Where(\"id = ?\", deliveryID)` (parameterized) throughout; existing offset pagination in driver_service.go:88 uses `offset := (page - 1) * pageSize`; existing saves check the error: `if err := s.db.WithContext(ctx).Save(&x).Error; err != nil { return err }`.", "gold": "# Code Review Findings: PR #412\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T18:30:00Z\n**Files Reviewed**: 1\n\n## Summary\nThe delivery reassignment changes introduce a SQL injection vector, package-level mutable global state in a service, a dropped error, missing context propagation, and an off-by-one pagination regression. The injection and global-state issues are direct CLAUDE.md violations. The location_consumer thundering-herd work is correctly out of scope and not flagged.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: SQL injection via string-concatenated ID in AssignDriver\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `services/delivery_service.go:24`\n**Issue**: The lookup builds the WHERE clause by concatenating `deliveryID` directly into the SQL string instead of using a bound parameter.\n**Evidence**:\n```go\nif err := s.db.WithContext(ctx).First(&delivery, \"id = '\"+deliveryID+\"'\").Error; err != nil {\n```\n**Why This Matters**: `deliveryID` originates from an HTTP route param; a crafted value can read or destroy delivery rows. GORM's parameterized form was already in use here before this PR.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Restore GORM bound parameter `\"id = ?\", deliveryID` | Matches codebase, zero injection surface | none |\n| B | Validate/escape deliveryID then concatenate | Keeps string form | Escaping is error-prone, still non-idiomatic |\n**Recommended**: Option A\n**Reasoning**: Every other service uses the `?` placeholder form; restoring it eliminates the vulnerability and matches the codebase convention exactly.\n**Recommended Fix**:\n```go\nif err := s.db.WithContext(ctx).First(&delivery, \"id = ?\", deliveryID).Error; err != nil {\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:54\ns.db.WithContext(ctx).Where(\"id = ?\", driverID).First(&driver)\n```\n\n### Finding 2: Package-level mutable global state in service\n**Severity**: HIGH\n**Category**: pattern-violation\n**Location**: `services/delivery_service.go:11`\n**Issue**: `activeDeliveryCache` is a shared, unsynchronized package-level map mutated by AssignDriver and CancelDelivery.\n**Evidence**:\n```go\nvar activeDeliveryCache = map[string]*models.Delivery{}\n```\n**Why This Matters**: CLAUDE.md states 'No global state in services.' The map is also written concurrently across request goroutines with no mutex — a data race that will panic under load.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Remove the cache entirely; read from DB | Honors CLAUDE.md, no race | Extra query (acceptable) |\n| B | Move cache behind a struct field + sync.RWMutex | Caching retained | Still a service-held cache; adds complexity for unclear benefit |\n**Recommended**: Option A\n**Reasoning**: The cache provides no demonstrated benefit and violates the no-global-state rule; the DB is the source of truth. EXTENDS/DUPLICATE/NEW classification: NEW abstraction with no prior art found via grep — and an unwanted one.\n**Recommended Fix**:\n```go\n// delete the var; remove activeDeliveryCache writes in AssignDriver and CancelDelivery\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:18 — services hold only db, no caches\ntype DriverService struct { db *gorm.DB }\n```\n\n### Finding 3: Dropped error and missing context in CancelDelivery save\n**Severity**: HIGH\n**Category**: bug\n**Location**: `services/delivery_service.go:62`\n**Issue**: `s.db.Save(&delivery)` ignores the returned error and omits `WithContext(ctx)`, so a failed cancel returns nil and the request cannot be cancelled/timed out.\n**Evidence**:\n```go\ns.db.Save(&delivery)\ndelete(activeDeliveryCache, deliveryID)\nreturn nil\n```\n**Why This Matters**: CLAUDE.md requires context propagation through all service calls; a silently failed save reports success to the caller and the audit trail diverges from DB state. AssignDriver's Save (line 30) has the same missing-WithContext issue.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `if err := s.db.WithContext(ctx).Save(&delivery).Error; err != nil { return err }` | Matches codebase, propagates ctx + error | none |\n| B | Log the error but continue | Keeps current flow | Still violates the rule, hides failures |\n**Recommended**: Option A\n**Reasoning**: Direct match to the established save pattern; restores both error handling and cancellation semantics.\n**Recommended Fix**:\n```go\nif err := s.db.WithContext(ctx).Save(&delivery).Error; err != nil {\n    return err\n}\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:71\nif err := s.db.WithContext(ctx).Save(&driver).Error; err != nil { return err }\n```\n\n### Finding 4: Off-by-one pagination regression in GetActiveDeliveries\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `services/delivery_service.go:40`\n**Issue**: Offset changed from `(page-1)*pageSize` to `page*pageSize`, skipping the entire first page for 1-based callers.\n**Evidence**:\n```go\noffset := page * pageSize\n```\n**Why This Matters**: Callers pass page=1 for the first page (per driver_service.go); this now returns the second page and silently drops results.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Restore `(page - 1) * pageSize` | Matches existing 1-based convention | none |\n| B | Document 0-based and update callers | Consistent if intentional | Out of scope, breaks existing handlers |\n**Recommended**: Option A\n**Reasoning**: Matches the existing pagination convention; Option B is an unrequested behavior change.\n**Recommended Fix**:\n```go\noffset := (page - 1) * pageSize\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:88\noffset := (page - 1) * pageSize\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | Yes |\n| HIGH | 2 | Yes |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n| DB writes via service layer | PASS | Logic stays in DeliveryService |\n| Context propagated through service calls | FAIL | Saves in AssignDriver & CancelDelivery omit WithContext |\n| No global state in services | FAIL | activeDeliveryCache package var |\n| Parameterized queries (idiomatic GORM) | FAIL | String-concatenated id in AssignDriver |\n| Money in integer cents | N/A | No money fields touched |\n| Table-driven tests for business logic | FAIL | No tests added for CancelDelivery |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| services/driver_service.go | 54 | Parameterized `id = ?` lookup |\n| services/driver_service.go | 71 | Save with WithContext + error check |\n| services/driver_service.go | 88 | 1-based offset pagination |\n| services/driver_service.go | 18 | Service struct holds only db |\n\n## Positive Observations\n- CancelDelivery correctly guards against cancelling a COMPLETED delivery before mutating state.\n- Status transitions use the existing string constants consistent with the model.\n- The reassignment logic was kept in the service layer, keeping handlers thin per convention.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T18:30:00Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
{"role": "code-review", "scenario": "gympass-access", "system": "You are the code-review agent for Archon, reviewing PRs in the gympass-access repo (Next.js 14 App Router, Drizzle ORM, PostgreSQL, multi-tenant by gym_id). Your job is to review a pull request for code quality, CLAUDE.md compliance, bug detection, and pattern adherence. You operate in four sequential phases: LOAD (read PR number from $ARTIFACTS_DIR/.pr-number, scope artifact, PR diff via gh pr diff, and CLAUDE.md), ANALYZE (check compliance, detect bugs, evaluate quality, match codebase patterns, check for primitive duplication), GENERATE (write a structured findings artifact to $ARTIFACTS_DIR/review/code-review-findings.md), and VALIDATE (verify the artifact exists and is structurally complete). You reference real codebase patterns as evidence for every finding via grep and provide multiple fix options with explicit reasoning. You never flag items listed in the scope's 'NOT Building' exclusion list. Verdict must be exactly one of APPROVE, REQUEST_CHANGES, or NEEDS_DISCUSSION.", "user": "Review PR #482 against gympass-access CLAUDE.md rules and codebase patterns. Produce a structured markdown artifact at $ARTIFACTS_DIR/review/code-review-findings.md. For each finding provide severity, location, evidence snippet, multiple fix options with pros/cons, a recommended fix, and a codebase pattern reference. Conclude with a verdict, statistics table, CLAUDE.md compliance table, and positive observations.\n\nResolved node inputs:\n- $ARTIFACTS_DIR/.pr-number => 482\n- scope.md: Feature is class booking with capacity enforcement. NOT Building (Scope Limits): waitlist auto-promotion (separate PR), email/SMS templating redesign, rate limiting on the bookings API.\n- CLAUDE.md key rules: Multi-tenant by gym_id (ALL DB queries MUST include a gym_id filter); class capacity enforced with Postgres advisory locks (pg_try_advisory_xact_lock) inside a transaction; Server Actions for mutations; snake_case DB, camelCase TS.\n- PR diff (src/features/bookings/actions.ts):\nclasses.findFirst now uses where: (c, { eq }) => eq(c.id, classId) with no gymId filter; booking count read then compared to capacity then insert with no advisory lock; db.insert(bookings) called without await; waitlist position recalculated via SELECT MAX(position)+1 with no lock; sendBookingConfirmation import present but call removed.", "gold": "# Code Review Findings: PR #482\n**Reviewer**: code-review-agent\n**Date**: 2026-06-08T14:22:10Z\n**Files Reviewed**: 1\n\n## Summary\nThis PR refactors `bookClass()` but introduces a tenant-isolation hole, leaves the capacity check non-atomic (the exact bug from issue #471), and drops two awaited/required side effects. The advisory-lock infrastructure exists in the codebase but is not used on this path. Significant rework required before merge.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Class lookup missing gym_id filter — multi-tenant isolation broken\n**Severity**: CRITICAL\n**Category**: security\n**Location**: `src/features/bookings/actions.ts:8`\n**Issue**: `classes.findFirst` filters only on `c.id`, so a member authenticated under gym A can book a class belonging to gym B by supplying its UUID.\n**Evidence**:\n```ts\nconst cls = await db.query.classes.findFirst({\n  where: (c, { eq }) => eq(c.id, classId),\n});\n```\n**Why This Matters**: CLAUDE.md mandates a `gym_id` filter on every query. This is a cross-tenant data-access vulnerability, not just a style issue.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Add `and(eq(c.id, classId), eq(c.gymId, gymId))` to the lookup | Minimal, matches existing pattern | None material |\n| B | Wrap all booking reads in a tenant-scoped repository helper | Centralizes the rule | Larger change, out of PR scope |\n**Recommended**: Option A\n**Reasoning**: Directly satisfies the CLAUDE.md gym_id rule with the smallest diff and mirrors the existing tenant-scoped query pattern; the repository abstraction (Option B) is worthwhile but belongs in its own PR.\n**Recommended Fix**:\n```ts\nconst cls = await db.query.classes.findFirst({\n  where: (c, { eq, and }) => and(eq(c.id, classId), eq(c.gymId, gymId)),\n});\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/classes/queries.ts:31 (grep \"eq(.*gymId\")\nwhere: (c, { and, eq }) => and(eq(c.id, id), eq(c.gymId, gymId)),\n```\n\n### Finding 2: Capacity check is non-atomic (TOCTOU) — advisory lock not acquired\n**Severity**: CRITICAL\n**Category**: bug\n**Location**: `src/features/bookings/actions.ts:11-22`\n**Issue**: The count read, capacity comparison, and insert run as separate statements with no transaction or advisory lock, so two concurrent requests can both pass the check and overbook. This is exactly issue #471.\n**Evidence**:\n```ts\nconst bookingCount = await db.select({ count: sql<number>`count(*)` })\n  .from(bookings)\n  .where(and(eq(bookings.classId, classId), eq(bookings.status, 'confirmed')));\nif (bookingCount[0].count >= cls.capacity) { /* waitlist */ }\n```\n**Why This Matters**: Violates the CLAUDE.md rule that capacity must be enforced with a Postgres advisory lock; produces data-integrity defects (double-booking a capacity-1 class).\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `db.transaction` + `pg_try_advisory_xact_lock(hashtext(classId))`, recheck count, insert | Correct, reuses existing lock helper | Requires wrapping the function body |\n| B | Add a partial unique index / capacity trigger in Postgres | DB-enforced invariant | Schema migration, broader scope |\n**Recommended**: Option A\n**Reasoning**: The advisory-lock primitive already exists in the codebase and CLAUDE.md prescribes it; Option A is the idiomatic, in-scope fix, whereas the DB-trigger approach is a separate hardening effort.\n**Recommended Fix**:\n```ts\nreturn db.transaction(async (tx) => {\n  await tx.execute(sql`SELECT pg_try_advisory_xact_lock(hashtext(${classId}))`);\n  const [{ count }] = await tx.select({ count: sql<number>`count(*)` })\n    .from(bookings)\n    .where(and(eq(bookings.classId, classId), eq(bookings.status, 'confirmed')));\n  // ...recheck capacity, then insert inside tx\n});\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/locks.ts:14 (grep \"pg_try_advisory_xact_lock\")\nawait tx.execute(sql`SELECT pg_try_advisory_xact_lock(hashtext(${key}))`);\n```\n\n### Finding 3: Missing await on booking insert — booking may not persist\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/features/bookings/actions.ts:25`\n**Issue**: `db.insert(bookings).values({...})` is not awaited, so the function can return `{ booked: true }` before the row commits; an error in the insert is unobserved.\n**Evidence**:\n```ts\ndb.insert(bookings).values({ memberId, classId, gymId, status: 'confirmed', bookedAt: new Date() });\nreturn { booked: true };\n```\n**Why This Matters**: Caller sees success for a booking that may have failed, corrupting capacity accounting and the member's UI state.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `await` the insert (inside the Finding-2 transaction) | Correct, trivial | None |\n| B | Add a lint rule for floating promises and fix | Prevents recurrence | Tooling change, separate PR |\n**Recommended**: Option A\n**Reasoning**: The await is required for correctness and naturally lives inside the transaction from Finding 2; the lint rule is a good follow-up but out of scope.\n**Recommended Fix**:\n```ts\nawait tx.insert(bookings).values({ memberId, classId, gymId, status: 'confirmed', bookedAt: new Date() });\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/waitlist/actions.ts:42 (grep \"await db.insert\")\nawait db.insert(waitlistEntries).values({ memberId, classId });\n```\n\n### Finding 4: Waitlist position computed without a lock — duplicate positions\n**Severity**: HIGH\n**Category**: bug\n**Location**: `src/features/bookings/actions.ts:18`\n**Issue**: The waitlist insert relies on `SELECT MAX(position)+1` semantics with no lock, so two concurrent joins can be assigned the same `position` integer.\n**Evidence**:\n```ts\nawait db.insert(waitlistEntries).values({ memberId, classId, joinedAt: new Date() });\n```\n**Why This Matters**: Duplicate positions break ordering and will break the auto-promotion feature (separate PR) that reads position ordering.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Acquire the same advisory lock and compute MAX(position)+1 inside the txn | Reuses lock from Finding 2 | None material |\n| B | Make position a `gym_id`/class-scoped sequence or unique index | DB-enforced uniqueness | Migration, broader scope |\n**Recommended**: Option A\n**Reasoning**: The waitlist insert already sits on the locked code path once Finding 2 is fixed; computing position inside the same transaction removes the race with no schema change.\n**Recommended Fix**:\n```ts\nconst [{ max }] = await tx.select({ max: sql<number>`coalesce(max(position),0)` })\n  .from(waitlistEntries).where(eq(waitlistEntries.classId, classId));\nawait tx.insert(waitlistEntries).values({ memberId, classId, position: max + 1, joinedAt: new Date() });\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/waitlist/actions.ts:40 (grep \"max(position)\")\nconst [{ max }] = await tx.select({ max: sql<number>`coalesce(max(position),0)` })...\n```\n\n### Finding 5: Booking confirmation no longer sent\n**Severity**: MEDIUM\n**Category**: bug\n**Location**: `src/features/bookings/actions.ts:1,28`\n**Issue**: `sendBookingConfirmation` is still imported but the call was removed during refactor; members no longer get email/push on a successful booking.\n**Evidence**:\n```ts\n// import sendBookingConfirmation present; no call before `return { booked: true }`\n```\n**Why This Matters**: Regression in expected behavior; the dangling import also signals an unfinished edit. Note: the email *templating redesign* is excluded by scope, but sending the existing confirmation is not.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Re-add `await sendBookingConfirmation(...)` after commit | Restores behavior | Side effect inside/after txn must be ordered post-commit |\n| B | Emit a Pusher/booking event and let a listener send | Decoupled | New infra, out of scope |\n**Recommended**: Option A\n**Reasoning**: Restores prior contract with minimal change; fire the confirmation after the transaction commits so a failed booking never sends a confirmation. The event-driven approach is a fine future refactor but is not in this PR's scope.\n**Recommended Fix**:\n```ts\nawait sendBookingConfirmation({ memberId, classId, gymId });\nreturn { booked: true };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/bookings/actions.ts (pre-PR) / src/features/waitlist/notify.ts:19\nawait sendBookingConfirmation({ memberId, classId, gymId });\n```\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 2 | No |\n| HIGH | 2 | Partially |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## CLAUDE.md Compliance\n| Rule | Status | Notes |\n| Every query filters by gym_id | FAIL | Finding 1 |\n| Capacity via Postgres advisory lock | FAIL | Finding 2 |\n| Mutations as Server Actions | PASS | bookClass is a server action |\n| snake_case DB / camelCase TS | PASS | naming consistent |\n| Promises awaited | FAIL | Finding 3 |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/features/classes/queries.ts | 31 | gym_id-scoped findFirst |\n| src/lib/locks.ts | 14 | pg_try_advisory_xact_lock in txn |\n| src/features/waitlist/actions.ts | 40-42 | awaited insert + locked MAX(position)+1 |\n\n## Positive Observations\n- Correctly imports `and, eq, sql` from drizzle-orm and uses parameterized `sql` (no string interpolation).\n- Status filter on the count query (`status = 'confirmed'`) is the right semantics for capacity.\n- Waitlisted-vs-booked return shape is clear and typed.\n\n## Metadata\n- Agent: code-review-agent\n- Timestamp: 2026-06-08T14:22:10Z\n- Artifact: $ARTIFACTS_DIR/review/code-review-findings.md"}
