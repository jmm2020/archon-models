{"role": "error-handling-review", "scenario": "invoicely-saas", "system": "You are an error-handling review agent for the invoicely-saas repository. Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, and CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, and fallback intent), and GENERATE (write a structured findings artifact). You never flag items listed in the scope.md 'NOT Building' exclusion section. You surface hidden errors — what each catch block could accidentally suppress — and provide multi-option fix suggestions grounded in existing codebase patterns, always citing the source file and lines of the pattern you reference.", "user": "Review PR #482's error handling. Load the PR diff and scope artifact, audit every error handler (try/catch, .catch, ||/??/optional chaining, error events) for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured findings artifact at $ARTIFACTS_DIR/review/error-handling-findings.md with findings, hidden-error risks, multi-option fixes, an audit table of every handler, statistics by severity, and a silent-failure risk assessment.\n\nResolved node inputs:\n- PR #482\n- scope.md 'NOT Building (Scope Limits)': distributed/Redis-backed rate limiting; pagination.\n- PR diff (src/api/controllers/clientController.ts): searchClients increments `loginAttempts[ip] || 0`, runs `await prisma.$queryRawUnsafe(...)` with no try/catch; deleteClient calls `prisma.client.delete(...)` WITHOUT await and immediately `res.status(204).send()`.", "gold": "# Error Handling Findings: PR #482\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T14:31:05Z\n**Handlers Reviewed**: 3\n\n## Summary\nThe diff has effectively no error handling: the un-awaited `prisma.client.delete` turns every delete failure into a silent success, and the awaited `$queryRawUnsafe` has no try/catch so DB/parse errors crash through as unhandled 500s. The `|| 0` fallback on the counter is benign. Overall error posture fails the project's \"surface and log failures\" expectation.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Un-awaited delete produces a silent failure (false 204)\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `src/api/controllers/clientController.ts:21`\n**Issue**: `prisma.client.delete(...)` is fire-and-forget; the 204 is returned before the promise settles, so any rejection is lost and the client is told the delete succeeded.\n**Evidence**:\n```ts\nprisma.client.delete({ where: { id, tenantId: req.tenantId } });\nres.status(204).send();\n```\n**Hidden Errors**:\n- `PrismaClientKnownRequestError P2025` (record not found) — user deletes a stale id, gets 204, row never existed; UI desyncs.\n- `PrismaClientKnownRequestError P2003` (FK constraint, client has invoices) — delete rejects in background, becomes an unhandled promise rejection.\n- Connection-pool / timeout errors — dropped entirely, no log, no metric.\n**User Impact**: Users believe a client was deleted when it was not, corrupting their mental model and downstream invoice references.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | await + try/catch, map P2025→404, P2003→409 | Correct status codes, logged, no silent loss | Most lines |\n| B | await only, delegate to central error middleware | Minimal diff | Needs middleware that maps Prisma codes |\n| C | await + generic 500 on any error | Simple | Loses precise 404/409 semantics |\n**Recommended**: Option A\n**Reasoning**: Matches the codebase's explicit Prisma-code mapping, gives the user accurate feedback, and logs the failure — satisfying CLAUDE.md's data-integrity emphasis. Central middleware (B) isn't proven to map P-codes; generic 500 (C) hides the not-found/conflict distinction.\n**Recommended Fix**:\n```ts\ntry {\n  await prisma.client.delete({ where: { id, tenantId: req.tenantId } });\n  res.status(204).send();\n} catch (err) {\n  const code = (err as { code?: string }).code;\n  if (code === 'P2025') return res.status(404).json({ error: 'Client not found' });\n  if (code === 'P2003') return res.status(409).json({ error: 'Client has invoices' });\n  logger.error('deleteClient failed', { id, tenantId: req.tenantId, err });\n  throw err;\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/api/controllers/invoiceController.ts:71-80\ntry {\n  await prisma.invoice.delete({ where: { id, tenantId: req.tenantId } });\n  res.status(204).send();\n} catch (err) {\n  if ((err as any).code === 'P2025') return res.status(404).json({ error: 'Invoice not found' });\n  logger.error('deleteInvoice failed', { id, err });\n  throw err;\n}\n```\n\n### Finding 2: searchClients DB call has no try/catch — unguarded 500\n**Severity**: HIGH\n**Category**: missing-logging\n**Location**: `src/api/controllers/clientController.ts:14`\n**Issue**: The awaited query has no surrounding try/catch and no logging; any DB error rejects the handler with no context, producing a bare 500 and no trace for ops.\n**Evidence**:\n```ts\nconst clients = await prisma.$queryRawUnsafe(`SELECT ... ILIKE '%${q}%' ...`);\nres.json(clients);\n```\n**Hidden Errors**:\n- SQL syntax error when `q` contains a quote — surfaces as opaque 500.\n- Connection/timeout errors — no log line, hard to triage in production.\n**User Impact**: Search silently 500s with no actionable error and no server-side breadcrumb.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Wrap in try/catch, log, return 500 with safe message | Logged + bounded response | Boilerplate per handler |\n| B | asyncHandler wrapper that logs + forwards to error middleware | DRY across controllers | Requires wrapper adoption |\n| C | Leave to global handler | No code | No per-handler context/logging |\n**Recommended**: Option B\n**Reasoning**: An asyncHandler wrapper centralizes logging and matches the existing controller pattern, avoiding per-handler boilerplate while guaranteeing every async route logs. (Pair with Finding-1 of the code-review: replace the raw query with Prisma.)\n**Recommended Fix**:\n```ts\nexport const searchClients = asyncHandler(async (req, res) => {\n  const clients = await prisma.client.findMany({\n    where: { tenantId: req.tenantId, name: { contains: String(req.query.q ?? ''), mode: 'insensitive' } },\n  });\n  res.json(clients);\n});\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/api/middleware/asyncHandler.ts:1-9\nexport const asyncHandler = (fn) => (req, res, next) =>\n  Promise.resolve(fn(req, res, next)).catch((err) => { logger.error(err); next(err); });\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n| clientController.ts:21 | un-awaited delete (no handler) | None | False 204 | None | FAIL |\n| clientController.ts:14 | await, no try/catch | None | Bare 500 | None | FAIL |\n| clientController.ts:7 | `loginAttempts[ip] || 0` fallback | N/A | 429 on threshold | OK (benign default) | PASS |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | Yes |\n| HIGH | 1 | Yes |\n| MEDIUM | 0 | — |\n| LOW | 0 | — |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n| Delete reports 204 but row not removed | High | High (data desync) | await + P2025/P2003 mapping (Finding 1) |\n| Search 500s with no server log | Medium | Medium (untriageable) | asyncHandler logging wrapper (Finding 2) |\n| Unhandled promise rejection crashes worker | Medium | High | await all Prisma calls |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/api/controllers/invoiceController.ts | 71-80 | Awaited delete with Prisma-code mapping + log |\n| src/api/middleware/asyncHandler.ts | 1-9 | Async route wrapper that logs and forwards |\n\n## Positive Observations\n- The `|| 0` default on the counter is a correct, safe fallback for a missing key.\n- Both handlers consistently scope by `req.tenantId`, so error paths don't risk cross-tenant leakage once handled.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T14:31:05Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "wayfarer-collab-api", "system": "You are an error-handling review agent for the wayfarer-collab-api project (TypeScript / Express 5 / Redis / Socket.IO). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate logging quality, user feedback, catch specificity, fallback intent), and GENERATE (write the structured findings artifact). You never flag items in scope.md \"NOT Building\". You surface hidden errors each catch could suppress and provide multi-option fixes grounded in existing codebase patterns, citing source file and lines. Verdict must be exactly APPROVE, REQUEST_CHANGES, or NEEDS_DISCUSSION.", "user": "MISSION: Review this PR's error handling. Load the PR diff and scope, audit every error handler for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured artifact at $ARTIFACTS_DIR/review/error-handling-findings.md with findings (severity, hidden-error risks, multi-option fixes with reasoning), a handler audit table, statistics by severity, and a silent-failure risk assessment.\n\nResolved node inputs:\n- PR number: 142\n- Linked GitHub issue (bug, P1): GET /api/trips/:id/presence calls Redis with no timeout and no fallback. When Redis is slow/down, the request hangs the full 30s socket timeout then returns a raw unhandled Redis connection error leaking stack traces. Expected: empty presence list with Retry-After, or a 503 with sanitized message.\n- scope.md NOT Building: presence TTL tuning; rate-limit changes.\n- CLAUDE.md error rule: no try/catch in routes; rely on central error-handling middleware; sanitized errors to clients.\n\nPR diff (presence.ts) adds: in-memory cache short-circuit on GET, then `await redis.smembers(...)`, then `raw.map(r => JSON.parse(r))`; heartbeat uses `safeParse` returning 400 on failure and persists `{userId, ip, lastSeen}`.", "gold": "# Error Handling Findings: PR #142\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:35:00Z\n**Handlers Reviewed**: 3\n\n## Summary\nThe PR adds no error handling around Redis on the GET path, so the production P1 (hang then leaked Redis stack trace) is not fixed. `JSON.parse` is also unguarded. The only handler present is the heartbeat `safeParse`, which is correct.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Redis call on GET presence has no timeout or fallback (the P1 bug persists)\n**Severity**: CRITICAL\n**Category**: unsafe-fallback\n**Location**: `src/routes/presence.ts:GET /:tripId/presence`\n**Issue**: `await redis.smembers(...)` has no timeout and no fallback. When Redis is slow/down the request hangs to the 30s socket timeout, then the raw connection error reaches the client. The added in-memory cache only short-circuits trips already warmed; a cold trip during an outage still hangs.\n**Evidence**:\n```ts\nconst raw = await redis.smembers(`presence:${tripId}`)\npresenceStore[tripId] = raw.map(r => JSON.parse(r))\n```\n**Hidden Errors**:\n- ECONNREFUSED / connection drop — triggers during rolling Redis restart; surfaces as raw 500 with stack trace.\n- Command timeout / slow Redis — request hangs 30s, no Retry-After.\n- Cluster MOVED/CLUSTERDOWN — leaks internal topology in error text.\n**User Impact**: ~800 failed requests over 4 min during the 2026-05-14 maintenance window; stack traces leaked to clients.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Promise.race timeout (e.g. 500ms) -> empty list + Retry-After | Meets issue's preferred behavior, graceful | Presence briefly empty during outage |\n| B | Wrap in helper that throws AppError(503) for central middleware | Sanitized, consistent | 503 instead of degraded list |\n| C | Both: timeout fast, on Redis-down throw typed 503 | Best of both | Most code |\n**Recommended**: Option A\n**Reasoning**: The issue explicitly prefers an empty presence list with `Retry-After`; presence is non-critical UX. It aligns with the timeout helper pattern already in the codebase and keeps the route free of try/catch per CLAUDE.md by using a typed helper.\n**Recommended Fix**:\n```ts\nconst raw = await withRedisTimeout(redis.smembers(`presence:${tripId}`), 500)\n  .catch(() => null)\nif (raw === null) { res.set('Retry-After', '5'); return res.json({ members: [] }) }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/redis.ts:62-71 — withRedisTimeout helper\nexport const withRedisTimeout = <T>(p: Promise<T>, ms: number) =>\n  Promise.race([p, new Promise<never>((_, r) => setTimeout(() => r(new RedisTimeout()), ms))])\n```\n\n### Finding 2: Unguarded JSON.parse can throw and bubble unhandled\n**Severity**: MEDIUM\n**Category**: silent-failure\n**Location**: `src/routes/presence.ts:GET`\n**Issue**: `raw.map(r => JSON.parse(r))` throws on a single malformed member, failing the whole response.\n**Evidence**:\n```ts\npresenceStore[tripId] = raw.map(r => JSON.parse(r))\n```\n**Hidden Errors**:\n- SyntaxError on corrupt/legacy member — triggered by a half-written value; whole request 500s.\n**User Impact**: Entire presence panel breaks because of one bad row.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | flatMap + safeParse, skip bad rows + log.warn | Resilient, observable | Silent drop of bad row |\n| B | Validate each with Zod member schema | Typed | More code |\n| C | Let it throw to middleware | Centralized | One bad row kills response |\n**Recommended**: Option A\n**Reasoning**: Logging the dropped row preserves debuggability (avoids true silent failure) while keeping the endpoint resilient; matches the warn-and-continue parsing pattern already used elsewhere.\n**Recommended Fix**:\n```ts\nconst members = raw.flatMap(r => { try { return [JSON.parse(r)] } catch (e) { log.warn({ tripId, r }, 'bad presence member'); return [] } })\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/pubsub.ts:33 — warn-and-skip on bad payload\ntry { handle(JSON.parse(msg)) } catch (e) { log.warn({ msg }, 'unparseable pubsub') }\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| GET smembers | none (bare await) | NONE | leaks raw 500 | N/A | FAIL |\n| GET JSON.parse map | none | NONE | 500 on bad row | N/A | FAIL |\n| heartbeat safeParse | branch (!success) | none | 400 Invalid body | specific | PASS |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | Yes |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Redis outage hangs request, leaks stack | High (recurs each maintenance) | Critical | Finding 1 timeout + Retry-After |\n| Corrupt member kills whole panel | Low | Medium | Finding 2 warn-and-skip |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/lib/redis.ts | 62-71 | withRedisTimeout race helper |\n| src/lib/pubsub.ts | 33 | warn-and-skip on parse failure |\n\n## Positive Observations\n- Heartbeat uses Zod `safeParse` with a clean 400 — correct boundary validation and user feedback.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T18:35:00Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "tapecast-feed", "system": "You are an error-handling review agent for the tapecast-feed repo (TypeScript · Fastify · Drizzle ORM · PostgreSQL · Cloudflare Workers · Vitest · Zod). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, fallback intent), GENERATE (write the structured findings artifact). You never flag items listed in scope.md 'NOT Building'. You surface hidden errors each catch could suppress and provide multi-option fixes grounded in existing codebase patterns, always citing source file and lines.", "user": "Mission: Review PR #412's error handling. Load the PR diff and scope artifact, audit every error handler (try/catch, .catch, ||/??/optional chaining, error events) for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured findings artifact at $ARTIFACTS_DIR/review/error-handling-findings.md with findings, hidden-error risks, multi-option fixes, an audit table of every handler, statistics, and a silent-failure risk assessment.\n\n$ARTIFACTS_DIR/.pr-number => 412\nscope.md NOT Building: webhook delivery (PR #420); CF edge-cache invalidation; historical backfill.\n\nPR diff (src/routes/feed.ts): adds module-level downloadCache; `const ip = request.headers['x-forwarded-for'] as string`; `const salt = process.env.SUBSCRIBER_SALT` (non-null assertion removed); `crypto.createHash('sha256').update(ip + salt + today)`; dedupe guarded by `if (!downloadCache[cacheKey])`; `new DownloadRepository().recordDownload(episodeId, hash)` is NOT awaited; `getAudioStream(episode.s3Key)` then `reply.send(stream)`.", "gold": "# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T00:00:00Z\n**Handlers Reviewed**: 3\n\n## Summary\nThe handler persists download counts via an un-awaited Promise with no `.catch`, swallowing every DB error, and relies on implicit `undefined` coercion for two header/env reads with no validation. Audio streaming has no error handling around the S3 fetch. These are silent-failure and unsafe-fallback risks.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Floating recordDownload Promise swallows all DB errors\n**Severity**: HIGH\n**Category**: silent-failure\n**Location**: `src/routes/feed.ts` (recordDownload call, inside isCountable branch)\n**Issue**: `recordDownload` returns a Promise that is neither awaited nor `.catch`-ed; any rejection is dropped silently.\n**Evidence**:\n```ts\nif (!downloadCache[cacheKey]) {\n  downloadCache[cacheKey] = Date.now();\n  new DownloadRepository().recordDownload(episodeId, hash);\n}\n```\n**Hidden Errors**:\n- Postgres unique-constraint violation (dedupe race across instances) — write lost, no log.\n- Connection-pool exhaustion / `ECONNREFUSED` during DB blip — download silently uncounted.\n- Drizzle query error (schema drift) — analytics silently degrade with zero signal.\n- UnhandledPromiseRejection — process-level noise / crash on newer Node.\n**User Impact**: Analytics under-report with no operator visibility; intermittent DB issues become invisible data loss.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await` inside try/catch, log via request.log.error | Errors observable, write ordered before reply | Small latency add |\n| B | Fire-and-forget with `.catch(err => request.log.error(...))` | Non-blocking response | Write not guaranteed before reply |\n| C | Enqueue to a durable retry queue, await enqueue | Resilient to DB blips | More infra than this PR scope |\n**Recommended**: Option A\n**Reasoning**: Aligns with CLAUDE.md's no-silent-failure rule and the already-awaited `findEpisode`; debugging value is high because the error surfaces in structured logs; effort is minimal.\n**Recommended Fix**:\n```ts\ntry { await new DownloadRepository().recordDownload(episodeId, hash); }\ncatch (err) { request.log.error({ err, episodeId, hash }, 'recordDownload failed'); }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/repositories/feedRepository.ts:40-46 — repo calls awaited and errors logged at route layer\ntry { await repo.write(...); } catch (err) { request.log.error({ err }, 'write failed'); }\n```\n\n### Finding 2: `process.env.SUBSCRIBER_SALT` unsafe fallback to undefined\n**Severity**: HIGH\n**Category**: unsafe-fallback\n**Location**: `src/routes/feed.ts` (salt assignment)\n**Issue**: Removing `!` lets `salt` be `undefined`; there is no guard, so the failure mode is a silently wrong hash rather than a thrown error.\n**Evidence**:\n```ts\nconst salt = process.env.SUBSCRIBER_SALT;\nconst hash = crypto.createHash('sha256').update(ip + salt + today).digest('hex');\n```\n**Hidden Errors**:\n- Missing env var — `undefined` coerces to `\"undefined\"`; no exception, all hashes collide.\n- Empty-string salt — passes any truthiness check yet weakens the privacy hash silently.\n**User Impact**: Severe under-counting (one download per episode/day) and a weakened privacy guarantee, with no error raised.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Validate at boot with Zod; throw if missing | Loud, fail-fast, centralized | Touches startup |\n| B | Guard inline: `if (!salt) throw fastify.httpErrors.internalServerError()` | Local, explicit | Per-call check, scattered |\n| C | Default salt fallback | Never throws | Catastrophic — silently wrong, do not do |\n**Recommended**: Option A\n**Reasoning**: A required secret should fail at startup, not per request; matches the repo's Zod-validated config and turns a silent analytics bug into an immediate, debuggable boot error.\n**Recommended Fix**:\n```ts\n// config.ts — throws at boot if absent\nexport const { SUBSCRIBER_SALT } = z.object({ SUBSCRIBER_SALT: z.string().min(16) }).parse(process.env);\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/config.ts:5-12 — env validated once at boot, throws on missing keys\nexport const env = envSchema.parse(process.env);\n```\n\n### Finding 3: Unguarded S3 audio stream fetch\n**Severity**: MEDIUM\n**Category**: missing-logging\n**Location**: `src/routes/feed.ts` (getAudioStream / reply.send)\n**Issue**: `getAudioStream(episode.s3Key)` is awaited but not wrapped; an S3/storage error rejects to Fastify's default 500 with no contextual log.\n**Evidence**:\n```ts\nconst stream = await new FeedRepository().getAudioStream(episode.s3Key);\nreturn reply.send(stream);\n```\n**Hidden Errors**:\n- S3 `NoSuchKey` (orphaned s3Key) — generic 500, no episode context logged.\n- Network timeout to object store — opaque 500, hard to triage.\n**User Impact**: Listeners get a bare 500 with no breadcrumbs for operators to correlate to an episode.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | try/catch around fetch; log episodeId+s3Key; reply 502/404 | Contextual logs, accurate status | Slightly more code |\n| B | Rely on a global Fastify errorHandler | DRY | Loses per-route context unless attached |\n**Recommended**: Option A\n**Reasoning**: Storage failures should be distinguishable (404 vs upstream 502) and logged with episode context for triage; consistent with how `findEpisode` already maps a miss to 404.\n**Recommended Fix**:\n```ts\nlet stream;\ntry { stream = await new FeedRepository().getAudioStream(episode.s3Key); }\ncatch (err) { request.log.error({ err, episodeId, s3Key: episode.s3Key }, 'audio fetch failed'); return reply.code(502).send(); }\nreturn reply.send(stream);\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/feed.ts:14 — episode miss mapped to explicit status, not a bare throw\nif (!episode) return reply.code(404).send();\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| recordDownload call | floating Promise (no await/.catch) | None | None | None | FAIL |\n| salt = process.env.SUBSCRIBER_SALT | implicit undefined fallback | None | None | None | FAIL |\n| ip = headers['x-forwarded-for'] as string | implicit undefined fallback (cast) | None | None | None | FAIL |\n| getAudioStream await | unguarded await → default 500 | None | Generic 500 | None | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 0 | - |\n| HIGH | 2 | Partial |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| DB write rejects unobserved | High | Under-counted analytics | Await + try/catch + structured log (F1) |\n| Missing salt → hash collision | Medium | Mass under-count + weak privacy | Zod boot validation (F2) |\n| S3 fetch error opaque 500 | Medium | Poor listener UX, hard triage | Wrap fetch, log context, map status (F3) |\n| Unhandled rejection crash | Medium | Process restart | Eliminated by F1 |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/repositories/feedRepository.ts | 40-46 | Awaited repo write with route-level error log |\n| src/config.ts | 5-12 | Zod boot env validation, throws on missing |\n| src/routes/feed.ts | 14 | Explicit status mapping instead of bare throw |\n\n## Positive Observations\n- `findEpisode` is awaited and its absence is explicitly mapped to a 404 rather than thrown.\n- DB access stays inside repository classes, keeping error surfaces consolidated.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T00:00:00Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "academytrack-lms-mobile", "system": "You are an error-handling review agent for the academytrack-lms-mobile repo (TypeScript / NestJS / TypeORM / BullMQ). Your sole job is to hunt the PR diff for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, and fallback intent), GENERATE (write the structured findings artifact). You never flag items in scope.md 'NOT Building'. Every finding includes a Hidden Errors list. Fix suggestions present multiple options with pros/cons and a justified recommendation. Every codebase pattern reference cites exact SOURCE file:lines. The Error Handler Audit table covers ALL handlers found. Verdict is exactly one of APPROVE, REQUEST_CHANGES, NEEDS_DISCUSSION. Output is a single markdown file at $ARTIFACTS_DIR/review/error-handling-findings.md.", "user": "Mission: Audit error handling in the compliance CSV export PR. Load the PR diff and scope artifact, audit every error handler for logging quality, user feedback, catch specificity, and fallback safety, then write $ARTIFACTS_DIR/review/error-handling-findings.md with findings (severity, hidden-error risks, multi-option fixes with reasoning), an audit table of every handler, statistics by severity, and a silent-failure risk assessment.\n\nResolved node inputs:\n- PR number: 412\n- scope.md 'NOT Building': PDF export; status-endpoint pagination; configurable columns; CSV header localization; retry/backoff policy tuning for the BullMQ queue (handled separately).\n- PR diff (same as code-review node): kickoffComplianceExport enqueues a BullMQ job and returns { jobId }; buildComplianceCsv runs a QueryBuilder `.getMany()`, computes daysOverdue, and concatenates a CSV string. Notably the diff contains NO try/catch, NO .catch(), and uses `e.completedAt ?? ''` (one nullish-coalescing handler). The S3 upload and presigned-URL generation referenced by the feature are not wrapped.", "gold": "# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:55:00Z\n**Handlers Found**: 1 explicit (`?? ''`) + 3 unguarded async boundaries\n\n## Summary\nThe diff has essentially no error handling: an enqueue call, a DB query, and (per the feature) an async S3 upload all run unguarded inside a BullMQ worker, so failures surface only as a generic job crash with no actionable log or user-facing status reason. The single `?? ''` fallback is benign. The dominant risk is silent/opaque failure of the export job.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: DB query and CSV build run unguarded inside the worker\n**Severity**: HIGH\n**Category**: missing-logging\n**Location**: `src/reports/reports.service.ts:buildComplianceCsv`\n**Issue**: `.getMany()` and the serialization loop have no try/catch. A query timeout, connection drop, or null relation throws an unlabeled error that BullMQ records as a stack trace with no PR/requester context.\n**Evidence**:\n```ts\nconst rows = await qb.getMany();\nfor (const e of rows) { /* dereferences e.user.name, e.course.name */ }\n```\n**Hidden Errors**:\n- `QueryFailedError` (MySQL timeout / lock wait) — large 50k scan under load.\n- `TypeError: Cannot read properties of null (reading 'name')` — an enrollment whose `user`/`course` join is null.\n- `ConnectionNotFoundError` — pool exhaustion during long export.\n**User Impact**: The status endpoint reports `failed` with no reason; the compliance officer cannot tell whether to retry or escalate.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Wrap builder in try/catch, log with `{ jobId, requesterId, rowCount }`, rethrow typed `ExportFailedError` | Structured context; preserves BullMQ retry | Slight verbosity |\n| B | Catch and return partial CSV | Job 'succeeds' | Silent data loss — unacceptable for compliance |\n| C | No catch; rely on global filter | Minimal | Loses per-job context |\n**Recommended**: Option A\n**Reasoning**: Compliance exports must never silently truncate (rules out B), and a global filter loses the requester/job context needed for debugging (rules out C). Option A matches the structured-logging pattern used elsewhere and keeps BullMQ's retry semantics intact.\n**Recommended Fix**:\n```ts\ntry {\n  const rows = await qb.getMany();\n  // ...build\n} catch (err) {\n  this.logger.error('compliance export failed', { jobId, requesterId, err });\n  throw new ExportFailedError('compliance-export', { cause: err });\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/exports/audit-export.service.ts:101-108\ncatch (err) { this.logger.error('audit export failed', { jobId, err }); throw new ExportFailedError('audit', { cause: err }); }\n```\n\n### Finding 2: Enqueue failure swallowed into an unhelpful 500\n**Severity**: MEDIUM\n**Category**: poor-user-feedback\n**Location**: `src/reports/reports.service.ts:kickoffComplianceExport`\n**Issue**: `await this.queue.add(...)` is unguarded; if Redis/BullMQ is unavailable the request returns a bare 500 with no indication the queue is down vs. a bad request.\n**Evidence**:\n```ts\nconst job = await this.queue.add('compliance-export', { requesterId });\nreturn { jobId: job.id };\n```\n**Hidden Errors**:\n- `Error: Connection is closed` (Redis down) — refresh-token Redis and queue share infra; an outage masks the cause.\n- `ReplyError: OOM` — Redis memory pressure rejecting the add.\n**User Impact**: Officer sees a generic failure and may retry-storm a degraded queue.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Catch and throw `ServiceUnavailableException` (503) with retry-after | Correct semantics; signals transient | One catch block |\n| B | Let it 500 | No code | Misleads client; no retry hint |\n| C | Fire-and-forget enqueue | Fast | Loses jobId guarantee |\n**Recommended**: Option A\n**Reasoning**: Queue unavailability is a transient infra condition; a 503 with retry-after gives the client honest, actionable feedback and prevents retry storms, unlike the opaque 500.\n**Recommended Fix**:\n```ts\ntry { const job = await this.queue.add('compliance-export', { requesterId }); return { jobId: job.id }; }\ncatch (err) { this.logger.warn('queue unavailable', { err }); throw new ServiceUnavailableException('Export queue unavailable, retry shortly'); }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/sync/sync.controller.ts:48-53\ncatch (err) { throw new ServiceUnavailableException('Sync temporarily unavailable'); }\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n| buildComplianceCsv `.getMany()` | unguarded await | None | None | N/A | FAIL |\n| buildComplianceCsv loop deref | unguarded | None | None | N/A | FAIL |\n| kickoffComplianceExport `queue.add` | unguarded await | None | Generic 500 | N/A | FAIL |\n| `e.completedAt ?? ''` | nullish coalescing | N/A | Empty cell (intended) | Narrow (null/undef only) | PASS |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| HIGH | 1 | No |\n| MEDIUM | 1 | Partial |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n| Export job dies with opaque stack trace | High | High (no compliance report, no reason) | Finding 1 typed error + context log |\n| Redis/queue outage masked as generic 500 | Medium | Medium | Finding 2 503 + retry-after |\n| Null user/course join crashes mid-build | Low | High (whole export fails) | try/catch + skip-or-fail policy with logged row id |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/exports/audit-export.service.ts | 101-108 | Typed ExportFailedError with context log |\n| src/sync/sync.controller.ts | 48-53 | 503 on transient infra failure |\n\n## Positive Observations\n- `e.completedAt ?? ''` is a correct, narrowly-scoped fallback that renders an empty cell rather than the string 'null'.\n- No broad `catch (e) {}` empty blocks and no `catch`-and-continue swallowing were introduced.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T18:55:00Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "venueops-ticketing", "system": "You are an error-handling review agent for the venueops-ticketing repo (TypeScript / Next.js 14 / Drizzle / PostgreSQL / offline PWA / Web Crypto). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, and CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, and fallback intent), and GENERATE (write a structured findings artifact). You never flag items listed in the scope.md 'NOT Building' exclusion section. You surface hidden errors and provide multi-option fix suggestions grounded in existing codebase patterns, always citing the source file and lines of the pattern you reference. Honor venueops conventions: scan decisions must be logged to append-only scan_events; the scan hot path must fail safe (reject, never silently admit).", "user": "Archon node: error-handling-review. Review the PR's error handling by loading the PR diff and scope artifact, auditing every error handler for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured findings artifact at $ARTIFACTS_DIR/review/error-handling-findings.md.\n\nResolved inputs:\n- $ARTIFACTS_DIR/.pr-number => 412\n- scope.md NOT Building: coordinator auth/RBAC, analytics dashboard, IndexedDB refresh strategy, cue asset selection.\n- PR diff (src/lib/scanner/validate-ticket.ts): a `try { JSON.parse } catch { return INVALID_FORMAT }` block; an un-awaited `verifyHmac` whose rejected promise would be unhandled; a bare `await db.insert(scanEvents)` with no try/catch; an `if (event && ...)` optional-style guard where a null event silently falls through to EVENT_ENDED.\n\nCodebase patterns (grep): src/lib/scanner/scan-tx.ts:33 wraps DB writes in try/catch and calls `logScanError(err, { ticketId, eventId })` then rethrows; src/lib/log.ts:11 `export function logScanError(err: unknown, ctx: ScanCtx)`; src/app/api/scan-validate/route.ts:38 awaits verifyHmac inside try/catch returning `{ status: 'CRYPTO_ERROR' }` on rejection.", "gold": "# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:35:00Z\n**Handlers Reviewed**: 4\n\n## Summary\nThe diff has one acceptable handler (JSON parse) but three error-handling defects: an un-awaited promise whose rejection is unhandled, an unguarded DB write with no logging, and a null-event path that silently degrades to a misleading EVENT_ENDED. The scan hot path can throw unlogged or admit/return incorrect statuses.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Un-awaited verifyHmac drops promise rejections (unhandled rejection)\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `src/lib/scanner/validate-ticket.ts:18`\n**Issue**: `verifyHmac(...)` is not awaited, so a thrown/rejected crypto operation produces an unhandled promise rejection that never reaches a catch, and the truthy Promise also skips the INVALID_SIGNATURE branch.\n**Evidence**:\n```ts\nconst sigValid = verifyHmac(parsed.ticketId + parsed.eventId, parsed.sig);\nif (!sigValid) return { status: 'INVALID_SIGNATURE' };\n```\n**Hidden Errors**:\n- `DOMException` (Web Crypto key import/verify failure) — when the signing key is malformed or unavailable; rejection is lost.\n- `TypeError` (undefined `parsed.sig`) — when QR payload omits sig; promise rejects silently.\n**User Impact**: Forged or malformed tickets pass through with no error surfaced; gate staff get no CRYPTO_ERROR feedback and may admit counterfeits.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await` inside try/catch returning CRYPTO_ERROR | Matches route.ts; surfaces failures | Slightly more code |\n| B | `await` only (no catch) | Fixes truthiness bug | Crypto throws still escape function |\n| C | `.catch(() => false)` | Compact | Swallows real crypto faults silently |\n**Recommended**: Option A\n**Reasoning**: Aligns with src/app/api/scan-validate/route.ts:38 which awaits inside try/catch and returns CRYPTO_ERROR. Preserves debugging value (logScanError) and gives staff actionable feedback; Option C reintroduces a silent failure, violating fail-safe scan rules.\n**Recommended Fix**:\n```ts\nlet sigValid: boolean;\ntry {\n  sigValid = await verifyHmac(parsed.ticketId + parsed.eventId, parsed.sig);\n} catch (err) {\n  logScanError(err, { ticketId: parsed.ticketId, eventId: parsed.eventId });\n  return { status: 'CRYPTO_ERROR' };\n}\nif (!sigValid) return { status: 'INVALID_SIGNATURE' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/app/api/scan-validate/route.ts:38\ntry { const valid = await verifyHmac(payload, sig); } catch { return { status: 'CRYPTO_ERROR' }; }\n```\n\n### Finding 2: Unguarded scan_events insert — failure throws unlogged\n**Severity**: HIGH\n**Category**: missing-logging\n**Location**: `src/lib/scanner/validate-ticket.ts:40`\n**Issue**: `await db.insert(scanEvents)...` has no try/catch. A DB error throws out of validateTicket with no logScanError context, losing the audit trail and crashing the scan response.\n**Evidence**:\n```ts\nawait db.insert(scanEvents).values({ ticketId: parsed.ticketId, eventId: parsed.eventId, scannedAt: new Date(), result: 'ADMITTED' });\n```\n**Hidden Errors**:\n- `DrizzleError`/`PostgresError` (connection drop / constraint) — during peak gate load; thrown unlogged.\n- Unique-constraint violation (if index added) — would surface as a raw throw instead of ALREADY_SCANNED.\n**User Impact**: Scanner shows a generic crash rather than a clear admit/reject; no diagnostic context for ops.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | try/catch -> logScanError + return DB_ERROR | Matches scan-tx.ts; preserves audit | More code |\n| B | onConflictDoNothing + check rowCount | Handles dup cleanly | Doesn't cover connection errors alone |\n| C | Combine A + B | Robust and pattern-aligned | Largest change |\n**Recommended**: Option C\n**Reasoning**: scan-tx.ts:33 wraps writes in try/catch with logScanError then rethrows; combining with onConflictDoNothing also fixes dedup atomicity. Maximizes debugging value and fail-safe behavior per CLAUDE.md append-only logging rule.\n**Recommended Fix**:\n```ts\ntry {\n  const r = await db.insert(scanEvents).values({...}).onConflictDoNothing({ target: [scanEvents.ticketId, scanEvents.eventId] });\n  if (r.rowCount === 0) return { status: 'ALREADY_SCANNED' };\n} catch (err) {\n  logScanError(err, { ticketId: parsed.ticketId, eventId: parsed.eventId });\n  return { status: 'DB_ERROR' };\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/scanner/scan-tx.ts:33\ntry { /* insert */ } catch (err) { logScanError(err, ctx); throw err; }\n```\n\n### Finding 3: Null event silently degrades to EVENT_ENDED\n**Severity**: MEDIUM\n**Category**: unsafe-fallback\n**Location**: `src/lib/scanner/validate-ticket.ts:48`\n**Issue**: `if (event && Date.now() < event.doorsClose.getTime())` treats a missing event identically to an ended event, conflating a data-integrity error with a normal expiry.\n**Evidence**:\n```ts\nconst event = await db.query.events.findFirst({ where: eq(events.id, parsed.eventId) });\nif (event && Date.now() < event.doorsClose.getTime()) return { status: 'ADMITTED' };\nreturn { status: 'EVENT_ENDED' };\n```\n**Hidden Errors**:\n- Missing/deleted event row — returns EVENT_ENDED instead of a NOT_FOUND/EVENT_MISSING signal; the real fault is masked.\n- Null `doorsClose` column — short-circuited by `event &&`, hiding a schema/data bug.\n**User Impact**: Staff see EVENT_ENDED for a configuration error and waste time at the gate; root cause never logged.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Explicit null check -> log + EVENT_MISSING | Distinguishes fault from expiry | New status value |\n| B | Reuse NOT_FOUND for null event | No new status | Less precise diagnostics |\n| C | Keep current fallback | No change | Masks real errors |\n**Recommended**: Option A\n**Reasoning**: Separating data faults from normal expiry preserves debugging value and aligns with the explicit-status discipline in scan-tx.ts; logging via logScanError gives ops the missing-event signal.\n**Recommended Fix**:\n```ts\nif (!event) { logScanError(new Error('event not found'), { ticketId: parsed.ticketId, eventId: parsed.eventId }); return { status: 'EVENT_MISSING' }; }\nif (Date.now() > event.doorsClose.getTime()) return { status: 'EVENT_ENDED' };\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/log.ts:11\nexport function logScanError(err: unknown, ctx: ScanCtx)\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| validate-ticket.ts:13 | try/catch (JSON.parse) | none | INVALID_FORMAT returned | narrow (parse only) | PASS |\n| validate-ticket.ts:18 | un-awaited promise | none | none (skips check) | n/a | FAIL |\n| validate-ticket.ts:40 | bare await insert | none | none (throws) | n/a | FAIL |\n| validate-ticket.ts:48 | `event &&` optional guard | none | misleading EVENT_ENDED | over-broad | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | 0 |\n| HIGH | 1 | 0 |\n| MEDIUM | 1 | 0 |\n| LOW | 0 | 0 |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Unhandled crypto rejection admits forgeries | High | Critical | await + try/catch -> CRYPTO_ERROR (Finding 1) |\n| DB insert failure crashes scan unlogged | Medium | High | try/catch + logScanError (Finding 2) |\n| Missing event masked as EVENT_ENDED | Low | Medium | explicit null check + log (Finding 3) |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/app/api/scan-validate/route.ts | 38 | await verifyHmac in try/catch -> CRYPTO_ERROR |\n| src/lib/scanner/scan-tx.ts | 33 | DB write try/catch -> logScanError -> rethrow |\n| src/lib/log.ts | 11 | logScanError(err, ctx) helper |\n\n## Positive Observations\n- The JSON.parse handler is correctly narrow and returns a specific INVALID_FORMAT status rather than swallowing the error.\n- No broad `catch (e) {}` empty blocks were introduced.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T18:35:00Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "formvault", "system": "You are an error-handling review agent for the FormVault repo (Python · FastAPI · SQLAlchemy 2.0 async · Postgres). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/except, .get() fallback, or default-value handler and evaluate logging quality, user feedback, catch specificity, and fallback intent), GENERATE (write the artifact). FormVault error rule: all user-facing errors wrapped in {error: string, code: string}. You never flag items in scope.md 'NOT Building'. You surface hidden errors each handler could suppress and provide multi-option fixes grounded in existing codebase patterns, always citing SOURCE file:lines.", "user": "Audit the PR's error handling. Load the diff and scope, evaluate every error handler / implicit fallback for logging, user feedback, specificity, and fallback safety. Write a complete artifact to $ARTIFACTS_DIR/review/error-handling-findings.md.\n\nPR #412. scope.md NOT Building: S3 attachment lifecycle, retention scheduler, React frontend.\n\nDiff (create_submission in app/routers/submissions.py): no try/except; uses `form.schema.get('required', [])`, `form.schema.get('approvalStages', [{}])[0]`, `first_stage.get('approverEmail')`, `payload.get('submitterEmail')`; two `await session.commit()` calls (raw INSERT + ORM row, then a second commit after status mutation) with no rollback; HTTPExceptions raised with plain string `detail`.", "gold": "File: $ARTIFACTS_DIR/review/error-handling-findings.md\n\n# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:35:00Z\n**Handlers Reviewed**: 5\n\n## Summary\nThe endpoint has no explicit error handling: two commits run without try/except or rollback, and several `.get()` defaults silently mask malformed form schemas. Combined with plain-string `HTTPException.detail`, this both hides DB failures and violates FormVault's {error, code} contract.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Commits without try/except leave session inconsistent on partial failure\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `app/routers/submissions.py` (commit blocks)\n**Issue**: The raw INSERT and ORM add are committed together; if either fails, there is no rollback and the second commit (status mutation) can run against a poisoned session.\n**Evidence**:\n```python\nsession.add(sub)\nawait session.commit()\nawait session.refresh(sub)\n...\nsub.status = 'stage_1_approved'\nawait session.commit()\n```\n**Hidden Errors**:\n- `IntegrityError` (duplicate/constraint on submission_data) → suppressed as a raw 500 with no rollback, leaving the session unusable.\n- `OperationalError`/`DBAPIError` (connection drop mid-commit) → no rollback; subsequent commit fails opaquely.\n- `PendingRollbackError` on the second commit → surfaces as an unrelated 500.\n**User Impact**: Client may receive a generic 500 while a partial write persisted, or a confusing error from the second commit unrelated to their request.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Single commit after all mutations, wrapped in try/except with rollback + logged {error, code} | Atomic, observable | Requires restructuring |\n| B | Keep two commits but add try/except/rollback around each | Minimal reshape | Still non-atomic |\n| C | Use `async with session.begin()` transaction context | Auto rollback on raise | Must remove manual commits |\n**Recommended**: Option C\n**Reasoning**: `session.begin()` gives automatic rollback on any exception, matches SQLAlchemy 2.0 async idiom, and removes the double-commit hazard entirely. Pair with a logged HTTPException carrying {error, code}.\n**Recommended Fix**:\n```python\ntry:\n    async with session.begin():\n        session.add(sub)\n        if sub.submitter_user_id == first_stage.get('approverUserId'):\n            sub.status = 'stage_1_approved'\nexcept IntegrityError as exc:\n    logger.warning('submission insert failed form=%s', form_id, exc_info=exc)\n    raise HTTPException(409, detail={'error': 'duplicate submission', 'code': 'SUBMISSION_CONFLICT'})\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/routers/forms.py:44\ntry:\n    async with session.begin():\n        session.add(form)\nexcept IntegrityError:\n    raise HTTPException(409, detail={'error': 'form exists', 'code': 'FORM_CONFLICT'})\n```\n\n### Finding 2: `.get('approvalStages', [{}])[0]` silently masks malformed schema\n**Severity**: MEDIUM\n**Category**: unsafe-fallback\n**Location**: `app/routers/submissions.py` (auto-advance block)\n**Issue**: A form with no `approvalStages` falls back to `[{}]`, so `first_stage.get('approverEmail')` is `None` and the branch silently does nothing — a misconfigured form looks like a working one.\n**Evidence**:\n```python\nfirst_stage = form.schema.get('approvalStages', [{}])[0]\nif payload.get('submitterEmail') == first_stage.get('approverEmail'):\n```\n**Hidden Errors**:\n- `KeyError`/empty-list would be the honest signal of a schema with `approvalStages: []`, but the `[{}]` default swallows it.\n- A `None == None` collision: if both sides are `None` the branch could fire on a schema-less form (when submitterEmail is also absent).\n**User Impact**: Forms with broken approval config accept submissions that never enter the intended approval flow, with no error surfaced to ops.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Validate `approvalStages` non-empty; 422 with {error, code} if missing | Surfaces misconfig | Stricter |\n| B | Guard the branch on truthy stage AND truthy approver id | Tolerant | Still hides empty config |\n| C | Default to `[]` and `if stages and ...` | Honest empty | Branch silently skipped |\n**Recommended**: Option A\n**Reasoning**: In a compliance product a form missing its approval config is a real error, not a silent no-op; failing fast with {error, code} gives ops the signal. Also closes the `None == None` collision noted above.\n**Recommended Fix**:\n```python\nstages = form.schema.get('approvalStages') or []\nif not stages:\n    raise HTTPException(422, detail={'error': 'form has no approval stages', 'code': 'FORM_MISCONFIGURED'})\nfirst_stage = stages[0]\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/services/approvals.py:31\nstages = form.schema.get('approvalStages') or []\nif not stages: raise ApprovalConfigError(form.id)\n```\n\n### Finding 3: HTTPException uses plain string detail, not {error, code}\n**Severity**: HIGH\n**Category**: poor-user-feedback\n**Location**: `app/routers/submissions.py` (404 and 422 raises)\n**Issue**: Errors are raised as `detail='Form not found'` / `detail=f'Missing field: {field}'`, violating the {error, code} contract clients depend on.\n**Evidence**:\n```python\nraise HTTPException(status_code=404, detail='Form not found')\nraise HTTPException(status_code=422, detail=f'Missing field: {field}')\n```\n**Hidden Errors**:\n- Clients parsing `detail.code` get `undefined`; programmatic error handling breaks silently on the frontend.\n- No machine-stable code means error categories can't be aggregated in logs/metrics.\n**User Impact**: Frontend cannot branch on a stable code; users see inconsistent messages and ops loses error taxonomy.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Wrap every detail in `{'error': msg, 'code': CODE}` | Matches contract | Touch each raise |\n| B | Add a global exception handler that reshapes detail | Centralized | Hides intent at call site |\n**Recommended**: Option A\n**Reasoning**: Explicit per-raise codes match CLAUDE.md and the existing forms router; a global reshaper can't invent meaningful codes after the fact.\n**Recommended Fix**:\n```python\nraise HTTPException(404, detail={'error': 'Form not found', 'code': 'FORM_NOT_FOUND'})\nraise HTTPException(422, detail={'error': f'Missing field: {field}', 'code': 'MISSING_FIELD'})\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/routers/forms.py:15\nraise HTTPException(404, detail={'error': 'Form not found', 'code': 'FORM_NOT_FOUND'})\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| commit blocks | none (uncaught) | None | Generic 500 | N/A | FAIL |\n| `form.schema.get('required', [])` | `.get` default | None | OK (validates after) | OK | PASS |\n| `form.schema.get('approvalStages', [{}])[0]` | `.get` default | None | None | Too loose | FAIL |\n| `first_stage.get('approverEmail')` | `.get` default | None | None | Too loose | FAIL |\n| 404 / 422 raises | HTTPException | None | Plain string detail | OK code, wrong shape | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | No |\n| HIGH | 1 | Yes |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Partial write on commit failure, no rollback | Medium | High | `async with session.begin()` (Finding 1) |\n| Misconfigured form silently skips approval | Medium | High | Fail-fast 422 (Finding 2) |\n| Frontend can't parse error code | High | Medium | {error, code} wrapping (Finding 3) |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| app/routers/forms.py | 44 | begin()/IntegrityError handling |\n| app/services/approvals.py | 31 | approvalStages presence guard |\n| app/routers/forms.py | 15 | {error, code} HTTPException |\n\n## Positive Observations\n- The `required` fields loop validates input before persistence rather than catching a downstream error.\n- 404 is raised before any DB mutation, avoiding orphaned state for the missing-form case.\n\n## Metadata\n- **Agent**: error-handling-agent\n- **Timestamp**: 2026-06-08T18:35:00Z\n- **Artifact**: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "docpod-unified", "system": "You are an error-handling review agent for the docpod-unified repository (FastAPI / SQLAlchemy 2.0 async / Celery). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/except, .catch, ||/??/optional-chaining/None-fallback handler and evaluate logging quality, user feedback, catch specificity, fallback intent), and GENERATE (write a structured findings artifact). You never flag items in scope.md 'NOT Building'. You surface hidden errors each handler could suppress and provide multi-option fixes grounded in existing codebase patterns, always citing SOURCE file:lines. Verdict is exactly one of APPROVE, REQUEST_CHANGES, NEEDS_DISCUSSION. Phase checkpoints PHASE_1/PHASE_2/PHASE_3 must pass in order.", "user": "Review the error handling of PR #482 ('Signed transcript delivery') by loading the PR diff and scope artifact, auditing every error handler for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured findings artifact at $ARTIFACTS_DIR/review/error-handling-findings.md with findings (severity, hidden-error risks, multi-option fixes with reasoning), an audit table of every handler, statistics by severity, and a silent-failure risk assessment.\n\nContext: This feature is the same pipeline implicated in GitHub bug 'signing task silently succeeds but produces a 0-byte PDF for transcripts >200KB' — root cause: BytesIO buffer read before seek(0), empty buffer stored to S3 with no error surfaced.\nscope.md NOT Building: HSM key rotation; /verify endpoint.\nCLAUDE.md error rules: Celery tasks must set explicit retry limits; never swallow exceptions silently; surface failures to Sentry; no bare except.\n\nPR DIFF (app/api/episodes.py): publish endpoint does `db.commit()` (un-awaited) then `sign_transcript_task.delay(episode_id)` with no try/except and no validation that the task was accepted; signed-transcript GET returns FileResponse(episode.signed_pdf_path) with no existence/size check (would happily serve a 0-byte file); list-by-org has no error handling on the DB call. No try/except, .catch, or explicit fallback appears in the diff — the risk is ABSENCE of handling around fallible async/Celery/S3-backed operations.", "gold": "# Error Handling Findings: PR #482\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T15:46:00Z\n**Handlers Reviewed**: 3 fallible operations, 0 explicit handlers present\n\n## Summary\nThe diff contains zero error handling around three fallible operations (async commit, Celery dispatch, FileResponse of an S3-backed path). The absence is itself the defect: the publish path can report success while persisting nothing, and the download path will serve the exact 0-byte PDF described in the open bug with no error surfaced.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Publish silently succeeds while commit and task dispatch are unguarded\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `app/api/episodes.py:38-40`\n**Issue**: `db.commit()` is not awaited (transaction never persists) and `sign_transcript_task.delay()` has no try/except; the endpoint returns `{'status': 'published'}` unconditionally.\n**Evidence**:\n```python\nepisode.status = 'published'\ndb.commit()\nsign_transcript_task.delay(episode_id)\nreturn {'status': 'published'}\n```\n**Hidden Errors**:\n- `MissingGreenlet` / un-awaited coroutine warning — commit never runs; no exception raised at call site.\n- `kombu.exceptions.OperationalError` — broker down; `.delay()` raises, but caller sees a 500 with no context after already returning success semantics.\n- Stale-state divergence: DB says `draft`, Flower says task queued.\n**User Impact**: Account owner is told the episode published; no signed PDF is ever produced and nothing reaches Sentry.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | `await db.commit()` only | Fixes persistence | Still no dispatch guard |\n| B | `await db.commit()` + try/except around `.delay()` logging to Sentry | Surfaces broker failures | Slightly more code |\n| C | Commit, then enqueue via existing `enqueue_task` helper that wraps retry+logging | Reuses pattern | Requires helper import |\n**Recommended**: Option C\n**Reasoning**: Aligns with the `enqueue_task` pattern, satisfies CLAUDE.md 'never swallow, surface to Sentry', and gives explicit retry semantics; debugging value is highest because broker failures become visible.\n**Recommended Fix**:\n```python\nepisode.status = 'published'\nawait db.commit()\ntry:\n    enqueue_task(sign_transcript_task, episode_id)\nexcept OperationalError:\n    logger.exception('sign dispatch failed', extra={'episode_id': episode_id})\n    raise HTTPException(503, detail='Publish saved; signing could not be queued')\nreturn {'status': 'published'}\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/tasks/dispatch.py:21-29\ntry:\n    task.delay(*args)\nexcept OperationalError:\n    logger.exception('enqueue failed'); raise\n```\n\n### Finding 2: signed-transcript download has no zero-byte / existence guard\n**Severity**: HIGH\n**Category**: unsafe-fallback\n**Location**: `app/api/episodes.py:21`\n**Issue**: `FileResponse(episode.signed_pdf_path)` trusts the stored path; a 0-byte or missing object is served as a valid response.\n**Evidence**:\n```python\nreturn FileResponse(episode.signed_pdf_path)\n```\n**Hidden Errors**:\n- 0-byte PDF (the open bug) served with 200 — verification consumer downstream fails opaquely.\n- `None` path → `TypeError` inside Starlette, surfacing as a raw 500.\n- S3 object deleted/expired → `FileNotFoundError` only at stream time.\n**User Impact**: Clients receive an empty 'signed' compliance document believing it valid.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Guard `if not path` → 404 | Cheap | Misses 0-byte case |\n| B | Guard path AND size>0 → else 409/502 with log | Catches the actual bug | Extra stat call |\n| C | Validate at sign-time only | Centralizes | Leaves read path trusting bad data |\n**Recommended**: Option B\n**Reasoning**: Defense-in-depth at the read path directly catches the documented 0-byte failure and surfaces it instead of serving a corrupt artifact; matches the size-assertion pattern already used in the document download route.\n**Recommended Fix**:\n```python\npath = episode.signed_pdf_path\nif not path or not await object_size(path):\n    logger.error('empty/missing signed pdf', extra={'episode_id': episode_id})\n    raise HTTPException(502, detail='Signed transcript not available')\nreturn FileResponse(path)\n```\n**Codebase Pattern Reference**:\n```python\n# SOURCE: app/api/documents.py:102-105\nif not blob or blob.size == 0:\n    raise HTTPException(502, detail='Document unavailable')\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| episodes.py:38 db.commit | none (un-awaited) | none | misleading success | n/a | FAIL |\n| episodes.py:40 task.delay | none | none | none | n/a | FAIL |\n| episodes.py:21 FileResponse | none | none | none | n/a | FAIL |\n| episodes.py:49 list-by-org execute | none | none | raw 500 on DB error | n/a | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 1 | Partial |\n| HIGH | 1 | Partial |\n| MEDIUM | 2 | No |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| Publish reports success, nothing persisted | High | Critical | await commit (Finding 1) |\n| 0-byte signed PDF served as valid | High | Critical | size guard (Finding 2) |\n| Broker outage swallowed | Medium | High | wrap .delay (Finding 1C) |\n| DB error → raw 500, no context | Medium | Medium | structured logging on list-by-org |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| app/tasks/dispatch.py | 21-29 | guarded Celery enqueue with logging |\n| app/api/documents.py | 102-105 | zero-byte download guard |\n\n## Positive Observations\n- 404 raised explicitly when the episode row is absent, rather than returning null.\n- No bare `except:` blocks were introduced anywhere in the diff.\n\n## Metadata\n- **Agent**: error-handling-agent\n- **Timestamp**: 2026-06-08T15:46:00Z\n- **Artifact**: `$ARTIFACTS_DIR/review/error-handling-findings.md`"}
{"role": "error-handling-review", "scenario": "podscout-analytics", "system": "You are an error-handling review agent for the podscout-analytics repo (TypeScript / Next.js 14 / Drizzle / tRPC v11 / Vercel KV). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, and CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, and fallback intent), and GENERATE (write a structured findings artifact). You never flag items listed in the scope.md 'NOT Building' exclusion section. You surface hidden errors -- what each catch block could accidentally suppress -- and provide multi-option fix suggestions grounded in existing codebase patterns, always citing the source file and lines of the pattern you reference. CLAUDE.md error rules: webhook handlers verify HMAC before processing; Vercel KV idempotency with 48h TTL; KV outage should log and fall through, not crash; dashboard pages have error boundaries.", "user": "Review the PR's error handling. Load the PR diff and scope artifact, audit every error handler (try/catch, .catch, ||/??/optional chaining, error events) for logging quality, user feedback, catch specificity, and fallback safety. Produce a complete structured findings artifact at $ARTIFACTS_DIR/review/error-handling-findings.md.\n\nPR number: 412. Scope.md 'NOT Building (Scope Limits)': pagination; KV caching of the retention result; Recharts component; cross-podcast aggregation.\nRelated open GitHub issue (bug, priority:medium): Webhook idempotency check throws UnhandledPromiseRejection when Vercel KV is unavailable -- `const seen = await kv.get(idempotencyKey);` has no try/catch; expected behavior is log and process the webhook anyway.\n\nSame analytics.ts PR diff as the code-review node (adds getRetentionCurve with an ownership check that throws `new Error('Forbidden')`, a `sessions.length === 0` guard, and `podcast?.ownerId` optional chaining). Audit the diff's error handling and assess silent-failure risk; the KV issue is contextual background for the silent-failure assessment but only diff handlers go in the audit table.", "gold": "# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:55:00Z\n**Handlers Reviewed**: 3\n\n## Summary\nThe diff introduces no try/catch but relies on optional chaining for an authorization decision and an early-return fallback on empty data. The ownership check throws an untyped `Error`, and the optional-chaining authz pattern is the only real risk. No KV handler appears in this diff (the KV crash lives in the webhook route, out of this PR), but the silent-failure assessment notes it as related context.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Untyped throw in authorization path masks intent and status\n**Severity**: HIGH\n**Category**: poor-user-feedback\n**Location**: `src/server/routers/analytics.ts:19`\n**Issue**: The authorization denial uses `throw new Error('Forbidden')`, which tRPC surfaces as `INTERNAL_SERVER_ERROR` (500), not `FORBIDDEN` (403). The client cannot distinguish an authz denial from an actual server fault.\n**Evidence**:\n```ts\nif (podcast?.ownerId !== ctx.session.user.id) {\n  throw new Error('Forbidden');\n}\n```\n**Hidden Errors**:\n- TRPCError typing loss: a genuine 500 elsewhere and this 403 both render identically -> on-call cannot triage.\n- Missing-podcast case: when `podcast` is `undefined`, `podcast?.ownerId` is `undefined`, the condition is true, and the user gets a 500 for a not-found podcast instead of a 403/404.\n**User Impact**: A user querying a podcast they do not own (or that does not exist) sees a generic server error; the dashboard error boundary shows a fault page rather than an access-denied state.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `throw new TRPCError({ code: 'FORBIDDEN' })` with explicit `!podcast` guard | Correct status, typed, distinguishes authz from fault | One import |\n| B | Return `[]` for non-owner | No error surface | Silently hides authz failure -- a silent failure |\n| C | `throw new TRPCError({ code: 'NOT_FOUND' })` when missing, FORBIDDEN when not owner | Most precise | Slightly more branching; may leak existence |\n**Recommended**: Option A\n**Reasoning**: A single FORBIDDEN for both missing and non-owned podcasts aligns with the codebase authz pattern, avoids leaking existence, gives correct status codes for debugging, and satisfies CLAUDE.md's protected-procedure contract. Option B is a silent failure; Option C leaks resource existence.\n**Recommended Fix**:\n```ts\nimport { TRPCError } from '@trpc/server';\nif (!podcast || podcast.ownerId !== ctx.session.user.id) {\n  throw new TRPCError({ code: 'FORBIDDEN' });\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/server/routers/podcasts.ts:54\nif (!row || row.ownerId !== ctx.session.user.id) {\n  throw new TRPCError({ code: 'FORBIDDEN' });\n}\n```\n\n### Finding 2: Empty-data early return is correct but undocumented intent\n**Severity**: LOW\n**Category**: unsafe-fallback\n**Location**: `src/server/routers/analytics.ts:30`\n**Issue**: `if (sessions.length === 0) return [];` is a legitimate guard against `Math.max(...[]) === -Infinity`, but the fallback intent (no data is a valid empty curve, not an error) is implicit.\n**Evidence**:\n```ts\nif (sessions.length === 0) return [];\n```\n**Hidden Errors**:\n- None suppressed: this path returns before any throwing code. The only risk is a future reader removing the guard and reintroducing the `-Infinity` loop bound.\n**User Impact**: None negative -- the client receives a valid empty array. Risk is purely maintainability.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Add a one-line comment explaining the -Infinity guard | Preserves intent, zero behavior change | None |\n| B | Leave as-is | No churn | Future regression risk |\n| C | Restructure to compute maxDuration defensively | Robust | More code for no behavior gain |\n**Recommended**: Option A\n**Reasoning**: The guard is correct; the only gap is explaining why it exists so a future edit does not remove it. A comment is the lowest-cost mitigation aligned with the codebase's inline-rationale style. Option C is over-engineering.\n**Recommended Fix**:\n```ts\n// Empty result is valid (no listeners); guard avoids Math.max(...[]) === -Infinity\nif (sessions.length === 0) return [];\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/server/routers/analytics.ts:72\n// Empty window is valid; return zeroed series rather than throwing\nif (rows.length === 0) return [];\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n| analytics.ts:13 (`podcast?.ownerId`) | optional-chaining | None | None | N/A (read) | PASS |\n| analytics.ts:19 (`throw new Error`) | throw | None | Untyped 500 | Too broad | FAIL |\n| analytics.ts:30 (`length === 0`) | guard/early-return | None | Valid empty | Specific | PASS |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| HIGH | 1 | Partial |\n| LOW | 1 | Yes |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n| Authz denial rendered as generic 500 (Finding 1) | High | Medium | Throw TRPCError FORBIDDEN |\n| Empty-curve guard removed in future edit | Low | Medium | Add explanatory comment (Finding 2) |\n| Related: webhook KV `kv.get` has no try/catch (out of this PR; see issue) | Medium | High | Wrap in try/catch, log, fall through -- track in webhook PR |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/server/routers/podcasts.ts | 54 | TRPCError FORBIDDEN authz throw |\n| src/server/routers/analytics.ts | 72 | Valid empty-result early return |\n\n## Positive Observations\n- The PR removes the original `episodeId ?? ''` fallback, which silently matched empty-string episode IDs -- a genuine silent-failure fix.\n- The `sessions.length === 0` guard correctly prevents an `-Infinity` loop bound and an unbounded loop.\n- No broad `catch (e) {}` swallow blocks were introduced.\n\n## Metadata\n- **Agent**: error-handling-agent\n- **Timestamp**: 2026-06-08T18:55:00Z\n- **Artifact**: `$ARTIFACTS_DIR/review/error-handling-findings.md`"}
{"role": "error-handling-review", "scenario": "eventpass-mobile-api", "system": "You are an error-handling review agent for the `eventpass-mobile-api` repo (TypeScript · Node/Express · Prisma · Redis · pino). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, and CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and evaluate each for logging quality, user feedback, catch specificity, and fallback intent), and GENERATE (write a structured findings artifact). You never flag items in scope.md 'NOT Building'. You surface hidden errors each catch could suppress and provide multi-option fixes grounded in existing codebase patterns, citing source file and lines. Repo conventions: structured JSON logging via pino; no silent swallows.", "user": "Review the PR's error handling. Load the PR diff and scope artifact, audit every error handler (try/catch, .catch, ||/??/optional chaining, error events) for logging quality, user feedback, catch specificity, and fallback safety. Produce the complete structured artifact at $ARTIFACTS_DIR/review/error-handling-findings.md.\n\nPR #214. scope NOT Building: offline batch sync; attendee export; X-Offline-Mode auth bypass.\n\nPR DIFF (same checkin.ts diff as the code-review node): adds module-level `recentCheckins` Map dedup, replaces `if (!payload.valid)` with `if (!payload)`, adds `if (!ticket || ticket.checkedInAt)`, awaits `prisma.ticket.findFirst` and `prisma.ticket.update` with NO surrounding try/catch, adds `checkInIp: req.ip`, returns `res.json(...)`. There is no error handler anywhere in the route — all awaits are unguarded.", "gold": "# Error Handling Findings: PR #214\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:34:00Z\n**Handlers Reviewed**: 2 (both are implicit fallback/optional patterns; zero try/catch present)\n\n## Summary\nThe route performs two unguarded `await` Prisma calls with no try/catch, so any DB/connection error becomes an unhandled rejection that Express returns as a bare 500 with no pino log. The new `if (!payload)` and `if (!ticket || ...)` are correct null-guards but mask distinct failure modes behind generic responses.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Unguarded Prisma awaits — DB errors crash silently with no log\n**Severity**: HIGH\n**Category**: missing-logging\n**Location**: `src/routes/checkin.ts:18`\n**Issue**: `prisma.ticket.findFirst` and `prisma.ticket.update` are awaited with no try/catch. A connection drop, constraint race, or timeout rejects the promise; Express emits a generic 500 and nothing is logged via pino.\n**Evidence**:\n```ts\nconst ticket = await prisma.ticket.findFirst({ where: { id: payload.ticketId, eventId } });\nawait prisma.ticket.update({ where: { id: ticket.id }, data: { checkedInAt: new Date(), ... } });\n```\n**Hidden Errors**:\n- `PrismaClientKnownRequestError P2002` (unique constraint race when two devices update same ticket) — surfaces as 500 instead of 409.\n- `PrismaClientInitializationError` (DB unreachable at the venue) — door staff see opaque failure with no audit trail.\n- `PrismaClientRustPanicError` / timeout — silent, unlogged.\n**User Impact**: Door staff get an undifferentiated error and cannot tell \"already in\" from \"DB down\"; ops has no log to triage at a live event.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Wrap both awaits in try/catch; map P2002→409, else log + 500 | Specific responses, logged | More lines |\n| B | Add a route-level asyncHandler wrapper that logs + 500 | DRY across routes | Loses P2002→409 specificity |\n| C | Do nothing, rely on global error middleware | Smallest | Still generic; convention wants per-route pino context |\n**Recommended**: Option A\n**Reasoning**: Matches the repo's pino-context convention, distinguishes the realistic two-device race (P2002) from infra failure, and preserves debugging value. Global middleware (B/C) cannot attach `eventId`/`ticketId` context.\n**Recommended Fix**:\n```ts\ntry {\n  await prisma.ticket.update({ where: { id: ticket.id }, data: { checkedInAt: new Date(), checkedInByDevice: deviceId, checkInIp: req.ip } });\n} catch (err) {\n  if (err instanceof Prisma.PrismaClientKnownRequestError && err.code === 'P2002') {\n    return res.status(409).json({ error: 'already_checked_in' });\n  }\n  req.log.error({ err, eventId, ticketId: payload.ticketId }, 'checkin_update_failed');\n  return res.status(500).json({ error: 'checkin_failed' });\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/routes/refund.ts:40-48\ntry { await prisma.payment.update(...); }\ncatch (err) { req.log.error({ err, paymentId }, 'refund_failed'); return res.status(500).json({ error: 'refund_failed' }); }\n```\n\n### Finding 2: Map fallback dedup silently degrades across replicas\n**Severity**: MEDIUM\n**Category**: unsafe-fallback\n**Location**: `src/routes/checkin.ts:14`\n**Issue**: The `lastSeen && Date.now()-lastSeen < 60_000` guard treats a missing Map entry as \"safe to proceed\", which is wrong when the request lands on a different worker than the first scan.\n**Evidence**:\n```ts\nconst lastSeen = recentCheckins.get(cacheKey);\nif (lastSeen && Date.now() - lastSeen < 60_000) { return res.status(409).json({ error: 'already_checked_in' }); }\n```\n**Hidden Errors**:\n- Cross-process miss — second worker has empty Map, so a true duplicate slips through as `accepted`.\n- Process restart — Map cleared mid-event, dedup window silently resets.\n**User Impact**: Same attendee admitted twice from two devices; the 409 the staff expect never fires.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n|---|---|---|---|\n| A | Replace Map with redis SET NX EX | Correct across replicas | Needs redis import |\n| B | Treat DB `checkedInAt`/P2002 as source of truth, drop Map | No new dep, no false fallback | Loses pre-DB short-circuit |\n**Recommended**: Option A\n**Reasoning**: Redis is the repo's idempotency layer; a missing-key fallback to \"proceed\" is only safe when the key store is shared.\n**Recommended Fix**:\n```ts\nif (!(await redis.set(`checkin:idem:${eventId}:${payload.ticketId}`, deviceId, 'EX', 60, 'NX')))\n  return res.status(409).json({ error: 'already_checked_in' });\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/idempotency.ts:12-19 — claim-once via redis SET NX EX\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n|---|---|---|---|---|---|\n| checkin.ts:18 findFirst await | unguarded await | None | generic 500 | none | FAIL |\n| checkin.ts:22 update await | unguarded await | None | generic 500 | none | FAIL |\n| checkin.ts:14 Map `lastSeen &&` | fallback guard | None | 409 | partial | FAIL |\n| checkin.ts:16 `if (!payload)` | null guard | None | 400 invalid_qr | OK | PASS |\n| checkin.ts:19 `if (!ticket || ...)` | null/optional guard | None | 409 | OK | PASS |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n|---|---|---|\n| CRITICAL | 0 | 0 |\n| HIGH | 1 | No |\n| MEDIUM | 1 | No |\n| LOW | 0 | 0 |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n|---|---|---|---|\n| DB error returns unlogged 500 | High at live venue | No triage data | try/catch + pino (Finding 1) |\n| P2002 race shown as 500 not 409 | Medium | Confusing staff UX | Map P2002→409 (Finding 1) |\n| Cross-worker duplicate admission | Medium | Double entry | Redis dedup (Finding 2) |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n|---|---|---|\n| src/routes/refund.ts | 40-48 | try/catch with pino context + 500 |\n| src/lib/idempotency.ts | 12-19 | Redis claim-once |\n\n## Positive Observations\n- `if (!payload)` and `if (!ticket || ticket.checkedInAt)` correctly prevent the previous null-deref crashes.\n- Distinct 400 (`invalid_qr`) vs 409 (`already_checked_in`) responses give staff actionable feedback.\n- No broad `catch (e) {}` swallows were introduced.\n\n## Metadata\n- **Agent**: error-handling-agent\n- **Timestamp**: 2026-06-08T18:34:00Z\n- **Artifact**: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "fleet-dispatch-service", "system": "You are an error-handling review agent for the fleet-dispatch-service Archon pipeline (Go / GORM / NATS JetStream). Your sole job is to hunt PR diffs for silent failures, dropped errors, broad catch/recover blocks, and unsafe fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every error return site, ignored `err`, `recover()`, and `||`/optional-style fallback, evaluating logging quality, caller feedback, specificity, and fallback intent), and GENERATE (write the structured findings artifact). You never flag items in scope.md 'NOT Building'. You surface hidden errors each ignored handler could suppress and provide multi-option fixes grounded in existing codebase patterns, citing source file and lines.", "user": "Mission: Review error handling in PR #412 (driver reassignment). Audit every error site in the diff for logging, caller feedback, specificity, and fallback safety. Write findings to $ARTIFACTS_DIR/review/error-handling-findings.md.\n\nscope.md NOT Building: location_consumer.go JetStream fix (#380); location history table; dashboard.\n\nCLAUDE.md error rules: every DB error must be returned to the caller; context must propagate so timeouts/cancellation surface as errors; no swallowed errors.\n\nPR diff (services/delivery_service.go) error sites: AssignDriver First() checks err (OK); AssignDriver `s.db.Save(&delivery)` — return value used, but call dropped WithContext; CancelDelivery First() checks err (OK); CancelDelivery `s.db.Save(&delivery)` — return value DROPPED entirely, then `return nil`. No logging anywhere. No NATS publish error handling present yet for the new fleet.delivery.reassigned path.\n\nCodebase pattern: services/driver_service.go:71 `if err := s.db.WithContext(ctx).Save(&driver).Error; err != nil { return err }`; events/publisher.go:33 `if err := p.nc.Publish(subj, data); err != nil { log.Error(...); return err }`.", "gold": "# Error Handling Findings: PR #412\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T18:34:00Z\n**Handlers Reviewed**: 4\n\n## Summary\nTwo DB lookups handle errors correctly, but both Save calls are defective: one drops the error entirely and the other loses context propagation. No logging exists on any path. The out-of-scope location_consumer work is not evaluated.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: CancelDelivery silently swallows Save error\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `services/delivery_service.go:62`\n**Issue**: `s.db.Save(&delivery)` discards the returned `*gorm.DB`/error and the function immediately `return nil`, reporting success regardless of outcome.\n**Evidence**:\n```go\ns.db.Save(&delivery)\ndelete(activeDeliveryCache, deliveryID)\nreturn nil\n```\n**Hidden Errors**:\n- `gorm.ErrInvalidTransaction` — when the connection/tx is already aborted; cancel reports success but row is unchanged.\n- driver/network errors (`pq` connection reset) — DB unreachable, caller told the delivery was cancelled.\n- `context.DeadlineExceeded` — would surface if WithContext were present; currently impossible to observe.\n- optimistic-lock / constraint violation — silently lost, audit table and delivery state diverge.\n**User Impact**: Ops marks a delivery cancelled in the UI; the record stays IN_PROGRESS in the DB, so a driver keeps an already-cancelled job.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `if err := s.db.WithContext(ctx).Save(&delivery).Error; err != nil { return err }` | Matches codebase, propagates ctx + error | none |\n| B | Capture err, log, then return it | Adds observability | Slightly more code |\n| C | Log and return nil | Keeps current signature | Still hides failure — rejected |\n**Recommended**: Option B\n**Reasoning**: Returning the error restores correctness (CLAUDE.md 'no swallowed errors') and matches driver_service.go:71; adding a log line gives the debugging value the codebase already expects on the publish path (publisher.go:33).\n**Recommended Fix**:\n```go\nif err := s.db.WithContext(ctx).Save(&delivery).Error; err != nil {\n    log.Error(\"cancel delivery save failed\", \"delivery_id\", deliveryID, \"err\", err)\n    return err\n}\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:71\nif err := s.db.WithContext(ctx).Save(&driver).Error; err != nil { return err }\n```\n\n### Finding 2: AssignDriver Save loses context propagation\n**Severity**: HIGH\n**Category**: unsafe-fallback\n**Location**: `services/delivery_service.go:30`\n**Issue**: The save error is checked, but the call dropped `WithContext(ctx)`, so request cancellation and timeout can no longer abort the write.\n**Evidence**:\n```go\nif err := s.db.Save(&delivery).Error; err != nil {\n    return nil, err\n}\n```\n**Hidden Errors**:\n- `context.DeadlineExceeded` / `context.Canceled` — never surfaces; a slow assign hangs past the request deadline.\n- runaway write during shutdown — no cancellation hook, write proceeds after the client disconnected.\n**User Impact**: Under DB slowness, assign requests block beyond their timeout instead of failing fast, holding goroutines and connections.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Re-add `WithContext(ctx)` | One-line, matches codebase | none |\n| B | Wrap in explicit timeout context | Bounded write | Redundant; ctx already carries deadline |\n**Recommended**: Option A\n**Reasoning**: The First() call two lines up already uses WithContext; restoring it on Save matches the established pattern and satisfies the context-propagation rule with minimal change.\n**Recommended Fix**:\n```go\nif err := s.db.WithContext(ctx).Save(&delivery).Error; err != nil {\n    return nil, err\n}\n```\n**Codebase Pattern Reference**:\n```go\n// SOURCE: services/driver_service.go:71\ns.db.WithContext(ctx).Save(&driver).Error\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n| delivery_service.go:24 (AssignDriver First) | err-return | none | returns err | generic | PASS |\n| delivery_service.go:30 (AssignDriver Save) | err-return | none | returns err | generic, no ctx | FAIL |\n| delivery_service.go:57 (CancelDelivery First) | err-return | none | returns err | generic | PASS |\n| delivery_service.go:62 (CancelDelivery Save) | dropped | none | none (return nil) | none | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | Yes |\n| HIGH | 1 | Yes |\n| MEDIUM | 0 | - |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n| Cancel reports success on DB failure | HIGH | HIGH | Return Save error (Finding 1) |\n| Assign ignores request timeout | MEDIUM | MEDIUM | Restore WithContext (Finding 2) |\n| No logging on any failure path | HIGH | MEDIUM | Add log.Error on returns per publisher.go:33 |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| services/driver_service.go | 71 | Save with WithContext + checked error |\n| events/publisher.go | 33 | Log then return on publish failure |\n\n## Positive Observations\n- Both First() lookups correctly check and return their errors.\n- CancelDelivery returns a descriptive error before mutating state when the delivery is COMPLETED.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T18:34:00Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
{"role": "error-handling-review", "scenario": "gympass-access", "system": "You are an error-handling review agent for the gympass-access repo (Next.js 14, Drizzle, PostgreSQL, multi-tenant by gym_id). Your sole job is to hunt PR diffs for silent failures, inadequate catch blocks, broad exception handlers, and inappropriate fallback behavior. You operate in three strict phases: LOAD (read PR number, scope.md, PR diff, CLAUDE.md error rules), ANALYZE (identify every try/catch, .catch(), ||/??/optional-chaining handler and the absence of handling around fallible IO, evaluating logging quality, user feedback, catch specificity, and fallback intent), and GENERATE (write the structured findings artifact). You never flag items in scope.md 'NOT Building'. You surface hidden errors each handler could suppress and provide multi-option fixes citing exact source file:lines of referenced patterns. Verdict is exactly one of APPROVE, REQUEST_CHANGES, NEEDS_DISCUSSION.", "user": "Audit error handling for PR #482 (gympass-access). Load the PR diff and scope, audit every error handler and every unguarded fallible operation for logging quality, user feedback, catch specificity, and fallback safety. Produce $ARTIFACTS_DIR/review/error-handling-findings.md with findings (severity, hidden-error risks, multi-option fixes with reasoning), a full handler audit table, statistics by severity, and a silent-failure risk assessment.\n\nResolved inputs:\n- PR #482, scope.md NOT Building: waitlist auto-promotion, email templating redesign, rate limiting.\n- CLAUDE.md error rules: mutations must surface actionable errors to the caller; no swallowed promise rejections; DB writes must be awaited so failures propagate.\n- PR diff (src/features/bookings/actions.ts): `bookClass()` has no try/catch; `db.insert(bookings).values({...})` is NOT awaited (floating promise); `bookingCount[0].count` dereferences an array index with no guard; class lookup throws 'Class not found' (the only explicit error); no advisory-lock acquisition result is checked.", "gold": "# Error Handling Findings: PR #482\n**Reviewer**: error-handling-agent\n**Date**: 2026-06-08T14:25:02Z\n**Handlers Reviewed**: 4 (1 explicit throw, 1 floating promise, 1 unsafe index access, 1 missing lock-result check)\n\n## Summary\n`bookClass()` has almost no error handling: the booking insert is a floating promise that can reject silently, an array index is dereferenced without a guard, and there is no try/catch translating DB failures into actionable caller errors. The single explicit `throw` is fine. This path can report success while having failed.\n**Verdict**: REQUEST_CHANGES\n\n## Findings\n### Finding 1: Floating booking insert swallows write failures\n**Severity**: CRITICAL\n**Category**: silent-failure\n**Location**: `src/features/bookings/actions.ts:25`\n**Issue**: `db.insert(bookings).values({...})` is not awaited, so a rejected write becomes an unhandled promise rejection while the function returns `{ booked: true }`.\n**Evidence**:\n```ts\ndb.insert(bookings).values({ memberId, classId, gymId, status: 'confirmed', bookedAt: new Date() });\nreturn { booked: true };\n```\n**Hidden Errors**:\n- Postgres unique/constraint violation — when a duplicate booking row is rejected; lost entirely.\n- Connection/pool timeout — under load the write rejects after the function already returned success.\n- Serialization failure inside the (intended) transaction — rolled back but caller told it succeeded.\n**User Impact**: Member sees a confirmed booking that does not exist; capacity counts drift; support tickets with no server-side error trail.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | `await` the insert and let it throw to the caller | Correct, minimal | Caller must handle thrown error (it should) |\n| B | `await` + try/catch that logs and returns a typed `{ error }` result | Actionable feedback, logged | Slightly more code |\n| C | Move insert inside a `db.transaction` that rejects on failure | Atomic with capacity check | Requires txn wrapper |\n**Recommended**: Option C (with B's logging at the boundary)\n**Reasoning**: The booking write must be atomic with the capacity check anyway, so awaiting it inside a transaction both fixes the floating promise and gives correct rollback; wrapping the call site to log and return a typed error matches the repo's server-action error convention and preserves debugging value per CLAUDE.md.\n**Recommended Fix**:\n```ts\ntry {\n  return await db.transaction(async (tx) => {\n    // ...locked capacity check...\n    await tx.insert(bookings).values({ memberId, classId, gymId, status: 'confirmed', bookedAt: new Date() });\n    return { booked: true };\n  });\n} catch (err) {\n  logger.error('bookClass failed', { gymId, classId, memberId, err });\n  return { error: 'BOOKING_FAILED' as const };\n}\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/waitlist/actions.ts:55-63\ntry { return await db.transaction(...); }\ncatch (err) { logger.error('joinWaitlist failed', { classId, err }); return { error: 'WAITLIST_FAILED' }; }\n```\n\n### Finding 2: Unsafe array-index dereference on count query\n**Severity**: MEDIUM\n**Category**: unsafe-fallback\n**Location**: `src/features/bookings/actions.ts:16`\n**Issue**: `bookingCount[0].count` assumes the result array is non-empty; if the query shape changes or returns no row, this throws an opaque `Cannot read properties of undefined`.\n**Evidence**:\n```ts\nif (bookingCount[0].count >= cls.capacity) { ... }\n```\n**Hidden Errors**:\n- Empty result set — `bookingCount[0]` is `undefined`; cryptic TypeError instead of a domain error.\n- Driver returning `count` as string — `>=` coerces unexpectedly; no explicit handling.\n**User Impact**: A 500 with an unhelpful message instead of a clear booking error; harder to triage.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Destructure with default: `const [{ count = 0 } = {}] = bookingCount` | Safe, concise | None |\n| B | Guard explicitly and throw a typed error if missing | Most explicit | Verbose |\n| C | Use `Number(count)` coercion plus default | Handles string counts | Slightly noisy |\n**Recommended**: Option A\n**Reasoning**: Matches the destructuring style used elsewhere in the feature, removes the undefined-deref class of bug with one line, and keeps the count numeric via the `sql<number>` typing; explicit guard (B) is overkill for an aggregate that always returns one row when correct.\n**Recommended Fix**:\n```ts\nconst [{ count = 0 } = {}] = bookingCount;\nif (count >= cls.capacity) { ... }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/features/classes/queries.ts:48\nconst [{ total = 0 } = {}] = await db.select({ total: sql<number>`count(*)` })...;\n```\n\n### Finding 3: Advisory-lock result never checked (missing handler)\n**Severity**: HIGH\n**Category**: missing-logging\n**Location**: `src/features/bookings/actions.ts:11` (lock absent on this path)\n**Issue**: No lock is acquired; once added, `pg_try_advisory_xact_lock` returns a boolean that must be checked — if `false`, the code should not proceed silently.\n**Evidence**:\n```ts\n// no pg_try_advisory_xact_lock call; capacity decided on unlocked read\n```\n**Hidden Errors**:\n- Lock contention — `pg_try_advisory_xact_lock` returns `false` and an unchecked path would race anyway.\n- Lock helper throwing on a closed connection — currently nothing would observe it.\n**User Impact**: Overbooking under concurrency with no log explaining why.\n#### Fix Suggestions\n| Option | Approach | Pros | Cons |\n| A | Use blocking `pg_advisory_xact_lock` so it always serializes | Simplest, no result to check | Can wait under heavy contention |\n| B | Use `pg_try_advisory_xact_lock`, on `false` log + return `{ error: 'BUSY' }` for retry | Non-blocking, observable | Caller must handle retry |\n| C | Try-lock with bounded retry loop | Smooth UX | More code |\n**Recommended**: Option B\n**Reasoning**: Matches the repo's existing try-lock convention, gives the caller an actionable `BUSY` signal, and logs contention so the silent-failure mode is eliminated; blocking (A) risks request pile-up the team has avoided elsewhere.\n**Recommended Fix**:\n```ts\nconst [{ locked }] = await tx.execute<{ locked: boolean }>(sql`SELECT pg_try_advisory_xact_lock(hashtext(${classId})) AS locked`);\nif (!locked) { logger.warn('booking lock contention', { classId }); return { error: 'BUSY' as const }; }\n```\n**Codebase Pattern Reference**:\n```ts\n// SOURCE: src/lib/locks.ts:14-19\nif (!locked) { logger.warn('lock contention', { key }); return { error: 'BUSY' }; }\n```\n\n## Error Handler Audit\n| Location | Type | Logging | User Feedback | Specificity | Verdict |\n| actions.ts:9 | `throw new Error('Class not found')` | No | Generic message | Specific | PASS |\n| actions.ts:25 | floating insert (no await) | No | None (false success) | None | FAIL |\n| actions.ts:16 | `bookingCount[0].count` index access | No | None (TypeError) | None | FAIL |\n| actions.ts:11 | missing advisory-lock result check | No | None | None | FAIL |\n\n## Statistics\n| Severity | Count | Auto-fixable |\n| CRITICAL | 1 | No |\n| HIGH | 1 | Partially |\n| MEDIUM | 1 | Yes |\n| LOW | 0 | - |\n\n## Silent Failure Risk Assessment\n| Risk | Likelihood | Impact | Mitigation |\n| Booking insert rejects after success returned | HIGH | CRITICAL | Await insert in transaction (Finding 1) |\n| Empty/odd count result throws opaque TypeError | LOW | MEDIUM | Safe destructure (Finding 2) |\n| Lock contention overbooks silently | MEDIUM | HIGH | Check try-lock result + log (Finding 3) |\n\n## Patterns Referenced\n| File | Lines | Pattern |\n| src/features/waitlist/actions.ts | 55-63 | txn + try/catch + typed error return |\n| src/features/classes/queries.ts | 48 | safe aggregate destructure |\n| src/lib/locks.ts | 14-19 | try-lock result check + warn log |\n\n## Positive Observations\n- The class-not-found case throws an explicit, specific error rather than returning a null booking.\n- No broad `catch (e) {}` swallow blocks were introduced.\n- No `||`/`??` fallbacks that mask undefined domain data.\n\n## Metadata\n- Agent: error-handling-agent\n- Timestamp: 2026-06-08T14:25:02Z\n- Artifact: $ARTIFACTS_DIR/review/error-handling-findings.md"}
